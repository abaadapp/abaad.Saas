# دليل النشر — Abad POS

## ⚠️ الخطوة الأهم: تفعيل المجدول (cron)

النظام فيه **4 مهام مجدولة** مكتوبة في [`routes/console.php`](routes/console.php)، لكنها **لا تعمل من نفسها**.
لارافيل يحتاج سطرًا واحدًا في cron يوقظ المجدول كل دقيقة، وبدونه تبقى المهام الأربع **معطّلة بصمت** — بلا رسالة خطأ، وبلا أي مؤشر في الواجهة.

### السطر المطلوب

أضِفه لجدول cron على الخادم (`crontab -e`):

```cron
* * * * * cd /path/to/abadpos && php artisan schedule:run >> /dev/null 2>&1
```

استبدل `/path/to/abadpos` بالمسار الفعلي للمشروع على الخادم.

> السطر يعمل **كل دقيقة**، ولارافيل هو من يقرّر أي مهمة حان وقتها. هذا هو السلوك الصحيح — لا تحاول جدولة كل مهمة في cron بنفسك.

### تأكّد أنه يعمل

```bash
php artisan schedule:list   # عرض المهام ومواعيدها القادمة
php artisan schedule:run    # تشغيل يدوي لما حان وقته الآن
```

---

## المهام المجدولة الأربع

| المهمة | الأمر | الموعد |
|---|---|---|
| نسخ احتياطي لكل المتاجر | `backup:run` | يوميًا 02:00 |
| تنبيه انخفاض المخزون | `alerts:low-stock` | يوميًا 08:00 |
| تنبيهات ذكية | `alerts:smart` | يوميًا 08:30 |
| تقرير الأداء الشهري | `reports:email` | أول كل شهر 07:00 |

المهام التي ترسل بريدًا تحتاج إعدادات `MAIL_*` صحيحة في `.env`، وإلا فشلت بصمت أيضًا.

---

## النسخ الاحتياطي

### أين تُحفَظ

```
storage/app/private/backups/YYYY-MM-DD/abadpos-backup-{business_id}-{timestamp}.json
```

### تشغيل يدوي

```bash
php artisan backup:run                 # كل المتاجر
php artisan backup:run --business=1    # متجر واحد
```

صاحب النشاط يقدر كذلك ينزّل نسخة متجره من **الإعدادات ← النسخ الاحتياطي**.

### ماذا تشمل النسخة (v2)

الفروع · العملات · المورّدون · أنواع المصروفات · الموظفون · التصنيفات · المنتجات · العملاء ·
الطلبات وأصنافها · أوامر الشراء وأصنافها · الكوبونات · المصروفات · المعاملات · حركات المخزون ·
الورديات · سجل النشاط · الإعدادات

### ما لا تشمله — وهذا مقصود

- **الاشتراكات والفواتير والباقات**: سجلات فوترة على مستوى المنصة. لو أمكن للمتجر استعادتها، لأمكنه إرجاع اشتراك منتهٍ أو تعديل فواتيره.
- **كلمات المرور ورموز التذكّر**: الملف يُنزَّل على جهاز المستخدم وقد يُرسل بالبريد. كتابة بصمات كلمات المرور فيه تعني أن تسريبه = تسريب حسابات.
- **الملفات المرفوعة** (شعارات، مرفقات المصروفات في `storage/app/public`): النسخة بيانات JSON فقط. **انسخ مجلد `storage/app/public` منفصلًا.**

### سلوك الاستعادة

- تعمل داخل *transaction* — لو فشلت في المنتصف تُلغى بالكامل ولا تترك بيانات نصف مستعادة.
- تحذف بيانات المتجر الحالية ثم تدرج بيانات الملف، بترتيب يراعي المفاتيح الأجنبية.
- **الموظفون استثناء**: يُحدَّثون ولا يُحذفون، **وحساب من ينفّذ الاستعادة لا يُمسّ إطلاقًا** — وإلا لحذف صاحب النشاط حسابه أثناء الاستعادة وخرج من النظام.
- الموظف الجديد يُنشأ بكلمة مرور عشوائية وتلزمه إعادة تعيين.

### ⚠️ النسخ تتراكم بلا حذف تلقائي

لا يوجد حاليًا تنظيف للنسخ القديمة — مجلد `backups/` ينمو يوميًا إلى ما لا نهاية.
إلى أن تُضاف مهمة تنظيف، أضِف حذفًا دوريًا للنسخ الأقدم من 30 يومًا:

```cron
0 3 * * * find /path/to/abadpos/storage/app/private/backups -type d -mtime +30 -exec rm -rf {} +
```

> **الأهم:** النسخ محفوظة على **نفس الخادم**. لو تعطّل القرص، ضاعت البيانات والنسخ معًا.
> انقلها دوريًا إلى تخزين خارجي (S3 أو خادم آخر) حتى تكون نسخة احتياطية حقيقية.

---

## قائمة تحقق قبل النشر

- [ ] `APP_ENV=production` و `APP_DEBUG=false`
- [ ] `APP_KEY` مولّد (`php artisan key:generate`)
- [ ] سطر cron مضاف ومُتحقَّق منه بـ `schedule:list`
- [ ] إعدادات `MAIL_*` صحيحة (وإلا تفشل مهام التنبيهات بصمت)
- [ ] `php artisan migrate --force`
- [ ] `npm run build`
- [ ] `php artisan storage:link` (لعرض الشعارات ومرفقات المصروفات)
- [ ] صلاحيات الكتابة على `storage/` و `bootstrap/cache`
- [ ] نسخ `storage/app/public` ضمن خطة النسخ الاحتياطي
- [ ] نقل النسخ إلى تخزين خارج الخادم
