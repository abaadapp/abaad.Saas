# البذور — نظام نظيف أم بيانات عرض

هذا الفرع (`ابعاد-2.0`) يبدأ بنظام **خالٍ من البيانات**.

## البذرة الافتراضية — نظيفة

```bash
php artisan migrate:fresh --seed
```

تُنشئ الحدّ الأدنى الذي يحتاجه النظام ليعمل فقط:

| ما يُنشأ | العدد | لماذا |
|---|---|---|
| الباقات | 3 | يحتاجها إنشاء المتاجر وشاشة الاشتراكات |
| مدير المنصة | 1 | للدخول إلى لوحة المنصة |
| متجر + مالكه | 1 + 1 | للدخول إلى لوحة المتجر |
| الفرع الرئيسي | 1 | نقطة البيع وحركات المخزون تحتاج فرعًا |
| العملة الأساسية (OMR) | 1 | واجهة العرض تحتاج عملة أساسية |
| الوظائف الافتراضية | 6 | مرتبطة بصلاحيات النظام |
| أنواع المصروفات | 7 | افتراضيات شاشة المصروفات |

**فارغ تمامًا:** المنتجات · التصنيفات · العملاء · الطلبات · المعاملات · المصروفات ·
حركات المخزون · المورّدون · أوامر الشراء · الكوبونات · الورديات · الاشتراكات ·
الفواتير · سجل النشاط · الإضافات · أرصدة الفروع.

### بيانات الدخول

| الحساب | البريد | كلمة المرور |
|---|---|---|
| مدير المنصة | `super@abadpos.com` | `password` |
| مالك النشاط | `admin@abadpos.com` | `password` |

غيّرها عبر `.env` قبل التشغيل:

```env
SEED_SUPER_EMAIL=...
SEED_SUPER_PASSWORD=...
SEED_ADMIN_EMAIL=...
SEED_ADMIN_PASSWORD=...
SEED_OWNER_NAME="..."
SEED_BUSINESS_NAME="..."
```

> ⚠️ لا يوجد في النظام مسار لإنشاء حساب جديد. لو حذفت كل المستخدمين فلن تستطيع
> الدخول إلا بإنشاء حساب من الطرفية عبر `php artisan tinker`.

---

## بيانات العرض الكاملة

البذرة القديمة محفوظة كما هي في `DemoSeeder` — 12 متجرًا ومنتجات وطلبات وعملاء
ومعاملات وسجلّات نشاط:

```bash
php artisan migrate:fresh          # قاعدة فارغة بلا أي بذرة
php artisan db:seed --class=DemoSeeder
```

للعودة إلى النظيف: `php artisan migrate:fresh --seed`
