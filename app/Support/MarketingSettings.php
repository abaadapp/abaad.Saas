<?php

namespace App\Support;

use App\Models\Setting;

/**
 * إعدادات أدوات التسويق — مفاتيحها وقيمها الافتراضية في موضعٍ واحد.
 *
 * كانت تُقرأ بمفاتيحَ نصّية متناثرة في المتحكّمات والقوالب، ولكلٍّ منها قيمة
 * افتراضية مكتوبة عند موضع القراءة. فحرفٌ يسقط من مفتاح لا يُخطئ أحدًا: تُقرأ
 * القيمة الافتراضية بهدوء وتبدو الشاشة سليمة، والإعداد الذي حفظه التاجر لا
 * أثر له.
 *
 * والمفاتيح هنا تُقرأ وتُكتب من هذا الباب وحده، فالافتراضيّ واحدٌ في الطرفين.
 */
class MarketingSettings
{
    /** المجموعات ومفاتيحها وقيمها الافتراضية */
    public const GROUPS = [
        /*
         * مفتاحٌ واحد — والسبعة الأخرى رُفعت لأنّها كانت تصف واجهةً لا وجود لها.
         *
         * «نشر الموقع» و«عرض الأسعار» و«قبول الطلبات» والجملة التعريفية والنبذة
         * وواتساب وإنستغرام: تُملأ وتُحفظ ولا يقرؤها شيء — لا صفحةَ متجرٍ في
         * النظام تعرضها لزائر. ومقبضٌ لا يُدير شيئًا أسوأ من غياب المقبض.
         *
         * وكذلك مجموعة `seo` كلّها: عنوانٌ ووصفٌ وكلماتٌ ومعرّفُ تحليلات ومفتاح
         * «اسمح بالفهرسة» — لصفحاتٍ لا يفهرسها محرّكٌ لأنّها غير منشورة أصلًا.
         *
         * والنطاق بقي وحده لأنّه وحده يُقرأ: زرٌّ في الشريط يفتح موقع التاجر
         * خارج النظام. وما حُفظ من المرفوع باقٍ في `settings` لم يُمحَ.
         */
        'website' => [
            /*
             * ومفتاحُ تفعيلٍ صريح — لا نطاقٌ فارغ.
             *
             * كان الإطفاء الوحيد أن يمحو التاجر نطاقه، فمن أراد أن يُخفي
             * موقعه شهرًا (يُعاد بناؤه، أو انتهى حجزُه) خسر ما كتبه ثمّ عاد
             * يبحث عنه. والتفعيل يقول ما يفعل: زرُّ الموقع في الترويسة،
             * وفحصُ «الظهور في البحث».
             *
             * واسمُه ليس `site_enabled`: ذاك مفتاحٌ ميّت من شاشةٍ قديمة كان
             * معناه «انشر متجري للزوّار» — ولا متجرَ يُنشر. وصفوفُه قد تبقى
             * في قاعدة متجرٍ رفعها يومًا أو في نسخةٍ تُستعاد، فإعادةُ اسمه
             * بمعنًى آخر تجعل قيمةً قديمة تُطفئ موقعًا لم يطلب صاحبُه إطفاءه.
             */
            'site_on' => '1',
            'site_domain' => '',

            /*
             * مفتاحٌ موروث لا يُقرأ — والطريق المختار يسكن `site_path`.
             *
             * كان يحمل «أيّ الطرق الثلاث اختارها التاجر»، ويقرؤه
             * `DomainOptions::mode` لتُرسل القيمة إلى شاشة الموقع — ولا تقرؤها
             * شاشة. ولا يكتبه اليوم إلا هجرةُ `a_domain_already_set_is_a_choice_made`.
             *
             * ويبقى معرَّفًا هنا ولا يُرفع: صفوفُه مكتوبةٌ في قواعد المتاجر،
             * ورفعُ المفتاح من المجموعة يجعل `save` تُسقطه بلا خبر لو كتبه أحدٌ
             * يومًا — وهو ما حدث لمجموعة `seo` قبلُ. والطريق الحيّ `site_path`.
             */
            'site_domain_mode' => '',

            /*
             * ولا `site_subdomain` هنا — رُفع، ولم يكن له كاتبٌ قطّ.
             *
             * عُرّف يوم كان يُظنّ أنّ الاسم المحجوز يسكن إعدادًا مستقلًّا. ثمّ
             * حُجز الاسمُ في عمودٍ له: `businesses.site_slug` — يُفحص تفرّدُه،
             * ويُعرض عنوانًا، ويُبنى عليه مسارُ متجر أبعاد. فبقي هذا المفتاح
             * مُعرَّفًا بلا شاشةٍ تكتبه ولا صفٍّ في قاعدة، ويقرؤه موضعان
             * فيردّان فراغًا دائمًا.
             *
             * ورفعُه هنا لا يُسقط قيمةً لأحد: `save` لا تُسقط إلا ما يُرسَل،
             * ولا مُرسِلَ له. انظر `PublishedSiteController::bySubdomain`.
             */

            /*
             * الطريقُ الذي اختاره التاجر إلى عنوانه: `sub` أو `own` أو `new`.
             *
             * وفراغُه يعني «لم يُسأل بعد» فتُعرض له البطاقاتُ الثلاث. ولا
             * يُستنتج هنا: `Storefront::path` تستنتجه لمن سبق الاختيار قبل
             * وجود السؤال — ومن حجز عنوانًا أو كتب نطاقه لا يُسأل من جديد.
             */
            'site_path' => '',

            /*
             * ومتجرُ أبعاد — مفاتيحه هنا، وكلٌّ منها **تقرؤه الصفحة العامّة**.
             *
             * ولا يُعاد ما رُفع يوم كانت هذه المجموعة تصف واجهةً لا وجود لها:
             * تلك كانت تُملأ وتُحفظ ولا يفتحها زائرٌ أبدًا. وهذه تُقرأ في
             * `Storefront::page` وتُرسم في `resources/views/store/show.blade.php`
             * — يفتح الزبون العنوان فيراها.
             *
             * والأسماء تبدأ بـ`store_` لا `site_`: `site_*` تصف موقعًا خارجيًّا
             * يملكه التاجر، و`store_*` تصف متجره عندنا. وخلطُهما يجعل إطفاء
             * أحدهما يُطفئ الآخر.
             */
            'store_on' => '0',
            'store_theme' => 'rose',
            'store_headline' => '',
            'store_about' => '',
            'store_show_prices' => '1',
            'store_whatsapp' => '',
            /*
             * حسابُ إنستغرام — يقرؤه بانِي الموقع في قسم «تواصل معنا».
             *
             * ولا يُخزَّن بعلامة «@»: الرابط يُبنى منه، و`@name` يصير
             * `instagram.com/@name` وهو عنوانٌ لا يفتح.
             */
            'store_instagram' => '',

            /*
             * هل يقبل الموقع طلبًا — لا هل يعرض ثمنًا.
             *
             * وسعرٌ مخفيٌّ لا يُطلب معه: «اطلب» على منتجٍ بلا سعر يعني زبونًا يطلب
             * ما لا يعرف ثمنه. والقاعدة في الخادم لا في الشاشة — الشاشة تُتخطّى.
             *
             * ═══ والافتراضيُّ '1' لأنّه صار مقروءًا ═══
             *
             * كان '0' ما دام لا يقرؤه شيء، فلا فرق. ثمّ دخل عقدَ النشر
             * (`Publication::commerce`) وصار يحكم ظهورَ زرّ الطلب في الموقع
             * المنشور — ولو بقي '0' لَاختفى الزرُّ من كلّ متجرٍ يعمل اليوم
             * عند أوّل نشرةٍ بعد هذه النسخة، بلا أن يطلب ذلك صاحبُه.
             *
             * والقاعدةُ عامّة: مفتاحٌ يُوصَل بقارئه يأخذ افتراضيَّ ما كان
             * يُرى، لا افتراضيَّ ما كان يُكتب.
             */
            'store_allow_orders' => '1',
            'store_pay_cod' => '1',
            'store_pay_transfer' => '0',
            'store_bank' => '',

            /*
             * والتوصيلُ — لمن في واجهته سلّةٌ وإتمامُ طلب (انظر `Store\WebCheckout`).
             *
             * الرسمُ والحدُّ الذي يسقط بعده مجّانًا، والمناطقُ التي يُوصَّل
             * إليها، وأوقاتُ التسليم، وساعاتُ العمل — كلُّها تُقرأ في الموقع
             * وحده ولا تمسّ الصندوق: رسمُ التوصيل هناك يُكتب باليد على كلّ
             * طلب. والفراغُ في الرسم يعني توصيلًا بلا رسم لا رسمًا مجهولًا.
             */
            'store_delivery_fee' => '',
            'store_free_delivery_over' => '',
            'store_delivery_areas' => '',
            'store_delivery_slots' => '',
            'store_hours' => '',
            'store_delivery_note' => '',

            /*
             * ═══ والصورةُ ليست وعدًا ═══
             *
             * محلُّ وردٍ يصوّر باقةً ثمّ يبيعها ثلاثين مرّة، وورد اليوم غيرُ
             * ورد أمس: صنفٌ نفد، ولونٌ لم يصل. فتصل الباقةُ مختلفةً قليلًا،
             * ويقيس الزبون ما في يده على صورةٍ ظنّها عقدًا.
             *
             * وهذا سطرُه ليقول ذلك قبل أن يدفع — لا بعد أن يفتح الصندوق.
             *
             * ═══ ولمَ لا يُكتب في «ملاحظة التوصيل» ═══
             *
             * تلك مشغولةٌ بما اسمُها: «التوصيل داخل مسقط خلال اليوم نفسه».
             * فجمعُهما يعني أن يختار التاجرُ أحدَهما — ويظهران في موضعين
             * مختلفين أصلًا: تلك تحت زرّ الإضافة، وهذا تحت الصورة نفسِها
             * وفوق زرّ الطلب وفي تأكيده.
             *
             * وفارغٌ افتراضًا: محلُّ كيكٍ أو عطرٍ لا يحتاجه، وسطرٌ يُفرض على
             * من لا يقصده ضجيجٌ في ثلاث صفحات.
             */
            'store_image_note' => '',

            /*
             * وكرتُ الهدية — صنفٌ يُباع لا خانةُ نصٍّ مجّانية.
             *
             * كان النصُّ يُكتب في كلّ طلبٍ بلا ثمن، والكرتُ يخرج من الدرج
             * ولا يُقرأ في تقريرٍ أبدًا. فصار خيارًا بثمنٍ يُكتب بندًا في
             * الفاتورة — انظر `Store\GiftCard`.
             *
             * وثمنُه فارغًا يعني الافتراضيّ (٥٠٠ بيسة) لا مجّانًا: من أراده
             * مجّانًا كتب صفرًا صريحًا، ومن تركه لم يقصد أن يُعطيه.
             */
            'store_gift_card' => '0',
            'store_gift_card_price' => '',

            /*
             * وحقولُ إتمام الطلب — ما يُعرض منها وما يُشترط.
             *
             * والفراغُ يعني «ما كان» لا قيمةً يخترعها النظام: متجرٌ يعمل
             * اليوم لا يتبدّل عليه شيءٌ بترقية. انظر `Store\CheckoutFields`.
             */
            'store_field_area' => '',
            'store_field_address' => '',
            'store_field_date' => '',
            'store_field_slot' => '',
            'store_field_recipient' => '',
            'store_field_promo' => '',
            'store_fulfil' => '',
            'store_max_days' => '',

            /*
             * وصفحةُ المتجر — ما فيها وترتيبُه، كما يضبطه صاحبُه.
             *
             * صورةُ الواجهة، وقسمٌ يكتبه بنفسه، وأصنافٌ يُبرزها، وترتيبُ
             * الأقسام وظهورُها. والفراغُ في كلٍّ يعني «ما كان» —
             * انظر `Store\StorePage`.
             */
            'store_hero_image' => '',
            'store_featured' => '',
            'store_sections' => '',
            'store_block_on' => '0',
            'store_block_title' => '',
            'store_block_text' => '',
            'store_block_image' => '',
            'store_block_cta' => '',
            'store_block_href' => '',
            /*
             * وصورةُ شريط المناسبات — كانت خطوطًا مائلةً مرسومةً بالـCSS.
             *
             * والشريطُ يَعِد بباقاتٍ وكرتِ هدية، ويُرسم إلى جانب وعده
             * مستطيلٌ مخطَّط. فيقرأ الزبون وعدًا ولا يرى منه شيئًا.
             */
            'store_banner_image' => '',
            /*
             * وسطرُ التذييل — «FLOWERS · LOUNGE · AND MORE».
             *
             * كان مكتوبًا في القالب بحروفه، فمحلٌّ آخر يلبس الواجهةَ نفسَها
             * يُذيّل صفحتَه بوصفِ محلٍّ ليس هو.
             */
            'store_tagline' => '',

            /*
             * ═══ وصفحاتُ المتجر — لا صفحةٌ واحدة ═══
             *
             * متجرُ الواجهة الخاصّة كان صفحةً ورفًّا وصنفًا وسلّة، وليس فيه
             * ما لسائر متاجر أبعاد: «من نحن» و«تواصل معنا» وقائمةٌ تصل
             * بينها. وهما ما يسأل عنهما الزبون قبل أن يدفع لمن لا يعرفه.
             *
             * والمفتاحُ يحمل ما أذِن به من الاختياريّتين مفصولًا بفاصلة،
             * وفراغُه «كلُّها» كـ`store_sections`. والإذنُ وحده لا يفتح
             * صفحة: ما لا نبذةَ فيه ولا هاتفَ لا يُعرض وإن أُذن به —
             * انظر `StoreNav::has`.
             */
            'store_pages' => '',

            /*
             * وصورةُ «من نحن» — أو نصٌّ وحده.
             *
             * وصفحةٌ بصورةٍ بلا نصٍّ لا تُفتح أصلًا: النصُّ هو الصفحة
             * والصورةُ زينتُها.
             */
            'store_about_image' => '',

            /*
             * ═══ وما يقرؤه غوغل — عنوانٌ ووصفٌ وإذنُ الفهرسة ═══
             *
             * كان عنوانُ الصفحة اسمَ النشاط ووصفُها أوّلَ مئةٍ وخمسين حرفًا
             * من نبذته — يُكتبان في `<head>` بلا أن يملك صاحبُه إليهما بابًا.
             * وسائرُ متاجر أبعاد لها شاشةُ «الظهور في البحث» تكتبهما.
             *
             * وفراغُهما يُبقي المحسوبَ كما كان: ترقيةٌ لا تُبدّل عنوانَ متجرٍ
             * مفهرَسٍ منذ شهور.
             *
             * و`store_seo_index` مفتاحُ «اظهر في نتائج البحث»: يُطفئه من
             * يبني متجره على عنوانٍ حقيقيّ ولا يريده مقروءًا بعد. ويُكتب
             * `noindex` في الصفحة — لا يُحذف شيء.
             */
            'store_seo_title' => '',
            'store_seo_desc' => '',
            'store_seo_index' => '1',
        ],
        /*
         * ولا مقبضَ هنا لا يقرؤه المُرسِل — ولا حدثَ يقرؤه ولا مقبضَ له.
         *
         * كانت المجموعة تحمل خمسةً أخرى بقيت من شاشة «افتح محادثة» بعد أن
         * حُذفت الشاشة: `wa_enabled` و`wa_number` وثلاثةُ نصوص رسائل. تُعرض
         * للتاجر مقابضَ كاملة — يُطفئ الإشعارات، ويكتب رقمه، ويصوغ نصّ
         * الرسالة ويعاينها على الشاشة — **ولا يقرأ منها أحدٌ حرفًا**.
         *
         * والإطفاء الحقيقيّ في عمود `businesses.whatsapp_enabled` يُدار من
         * بطاقة الوصلة فوقها، والرقم رقمُ الوصلة المعتمدة عند ميتا، والنصّ
         * قالبٌ معتمَدٌ باسمه (انظر WhatsAppTemplates) — لأنّ ميتا لا تقبل
         * نصًّا حرًّا في رسالةٍ يبدؤها العمل.
         *
         * فمقبضٌ لا يُدير شيئًا أسوأ من غياب المقبض: التاجر يُطفئ الإشعارات
         * ويطمئنّ، وهي تُرسل.
         */
        /*
         * علاماتُ البدء — «ضُغط ربط مع أبعاد» لكلّ أداة.
         *
         * ومجموعةٌ وحدها لا مفتاحٌ في مجموعة الأداة: تلك عقدُها أنّ كلَّ
         * مفتاحٍ فيها يقرؤه مُرسِل الرسائل، وحارسٌ يفحص ذلك. وهذه حالُ شاشةٍ
         * لا إعدادُ إرسال — فوضعُها هناك يُسقط الحارس أو يُضعِفه، وإضعافُ
         * حارسٍ لإدخال مفتاحٍ لا يخصّه أسوأ من المفتاح نفسه.
         *
         * والاستنتاج لا يغني عنها: `whatsapp_enabled` افتراضُه `true` في
         * القاعدة فكلُّ متجرٍ «بدأ» من لحظة إنشائه، ومفتاحُ الخرائط في
         * المنصّة يُتمّ الخطوة الأولى للجميع. انظر MarketingController::connect.
         */
        'connect' => [
            'wa_setup_started' => '0',
            'google_setup_started' => '0',
        ],
        /*
         * ═══ ومفاتيحُها هي `WhatsAppEvent::SETTING_KEYS` كلُّها — لا بعضُها ═══
         *
         * وكانت أربعةً والأحداثُ ستّة. فالشاشة ترسم ستّةَ مقابض (تقرؤها من
         * `WhatsAppEvent::ALL`)، واثنان منها لا مفتاحَ لهما هنا: يُضغطان
         * فلا يُحفظان، ويُقرآن فيعودان مطفأين دائمًا. وأبعدُ من ذلك: مُرسِلُ
         * التذكيرات يسأل هذه المجموعةَ نفسَها، فتذكيرُ الفاتورة **لم يكن
         * يخرج لمتجرٍ واحد قطّ** مهما فعل صاحبُه.
         *
         * والحارسُ الذي كان يقيس اتّجاهًا واحدًا («كلُّ مفتاحٍ هنا يقرؤه
         * المُرسِل») لا يرى ذلك: النقصُ في الاتّجاه الآخر. انظر
         * MarketingKeepsItsPromisesTest — صار يقيس الاتّجاهين.
         */
        'whatsapp' => [
            'wa_on_order' => '1',
            'wa_on_ready' => '1',
            'wa_on_out_for_delivery' => '1',
            'wa_on_delivered' => '0',
            /*
             * وتذكيرُ الفاتورة مطفأٌ حتى يُطلب.
             *
             * رسائلُ الطلب يتوقّعها الزبون لأنّه اشترى للتوّ؛ والمطالبةُ
             * بالسداد رسالةٌ من نوعٍ آخر تخرج باسم التاجر إلى عميلٍ قد
             * يكون رتّب أمرَه معه بالهاتف. فإشعالُها من تلقائنا يُطالب
             * نيابةً عن صاحب المحلّ بما لم يقله.
             */
            'wa_on_invoice_due_soon' => '0',
            'wa_on_invoice_overdue' => '0',
        ],
        /*
         * ربطُ خرائط Google — أربعةُ مفاتيح، وكلٌّ منها يقرؤه شيء.
         *
         * `google_place_id` مصدرُ الروابط كلّها، و`google_maps_url` ما لصقه
         * التاجر يُحفظ كما هو ليراه في حقله فيعرف بمَ ربط، و`…_on_receipt`
         * يقرؤه الإيصال فيطبع رمزًا يمسحه الزبون، و`google_api_key` مفتاحُ
         * «Places API (New)» الذي تُسحب به التقييمات — يُقرأ في `GoogleReviews::pull`.
         *
         * والمفتاحُ **معمًّى** في القاعدة ولا يُرسل إلى الشاشة: يُكتب مرّةً
         * ويُعرض منه آخرُ أربعةِ أحرف. وإعداداتُ المتاجر تُقرأ في شاشاتٍ
         * وتُصدَّر في نُسَخٍ احتياطية، ومفتاحٌ مكشوفٌ فيها فاتورةٌ يدفعها
         * صاحبُه على نداءات غيره.
         */
        /*
         * ولا `google_place_id` ولا `google_maps_url` هنا بعد اليوم.
         *
         * انتقلا إلى `branch_google_places` بهجرة
         * `a_branch_has_its_own_place_on_the_map`، ونقلت الهجرةُ ما كان
         * محفوظًا إلى أقدم فرعٍ في كلّ متجر. فلا قيمةَ تُفقد — والصفوفُ
         * القديمة باقيةٌ في `settings` لم تُمحَ.
         *
         * ورفعُهما من هنا مقصود: لو بقيا لَقرأتهما الشاشةُ من موضعٍ ويكتبهما
         * الفرعُ في موضعٍ آخر — وحقلان يقولان الشيء نفسه يفترقان يومًا،
         * فيُطبع على الإيصال معرّفٌ غيرُ الذي تعرضه الشاشة.
         *
         * والمقبضُ يبقى للمتجر: «هل يُطبع رمزُ التقييم؟» سياسةٌ واحدة،
         * والرابطُ يُقرأ من فرع الورقة.
         */
        'google' => [
            'google_review_on_receipt' => '0',
            /*
             * عرضُ معدّل Google في صفحة المتجر — مطفأٌ حتى يُطلب.
             *
             * إشعالُه من تلقائنا يضع رقمًا على واجهة متجرٍ لم يطلب صاحبُه
             * وضعه؛ ومعدّلٌ منخفضٌ يُعرض بلا إذنٍ ضررٌ مباشر.
             */
            'google_show_on_site' => '0',
            'google_api_key' => '',

            /*
             * تنبيهُ التقييم المنخفض — والحدُّ من ثلاثةٍ لا من عشرة.
             *
             * وهو مُشغَّلٌ افتراضًا: تقييمٌ بنجمةٍ يُترك يومين يقرؤه كلُّ من
             * يفتح ملفَّ المحلّ. ومن أراد الهدوء يُطفئه بمقبضه.
             */
            'gbp_alerts_enabled' => '1',
            'gbp_low_rating' => '2',
        ],
        /*
         * الظهور في البحث — مفتاحٌ واحد، وهو الذي يُقرأ.
         *
         * ولا تعود مجموعة `seo` القديمة: عنوانٌ ووصفٌ وكلماتٌ مفتاحية ومفتاح
         * «اسمح بالفهرسة» تُملأ عندنا ولا تصل صفحةً يقرؤها محرّك — موقعُ
         * التاجر خارج النظام. وحقلٌ يُملأ ولا يصل أسوأ من غيابه: يظنّ صاحبُه
         * أنّه ضبط سيو موقعه.
         *
         * ومعرّفُ القياس يُقرأ في موضعين: يُبنى منه الوسمُ الذي يلصقه في
         * موقعه، ويُبحث عنه في صفحته لتُقال حالةُ الربط بالحقيقة لا باللصق.
         */
        'seo' => [
            'ga_measurement_id' => '',
        ],
        'loyalty' => [
            'loyalty_enabled' => '1',
            'loyalty_earn_rate' => '5',
            'loyalty_redeem_max_pct' => '50',
            'loyalty_redeem_min' => '100',
        ],
    ];

    /**
     * ما قُرئ في هذا الطلب — فلا يُسأل الجدولُ عن المجموعة نفسِها مرّتين.
     *
     * ═══ والعطبُ الذي وُضع لأجله ═══
     *
     * صفحةُ متجرٍ واحدةٌ يفتحها زائر كانت تسأل هذا الاستعلامَ **سبعَ عشرةَ
     * مرّة**: القائمةُ تسأل عن كلّ صفحةٍ أهي قائمة، وكلُّ سؤالٍ يقرأ الهويّة،
     * والهويّةُ تقرأ المجموعة؛ ثمّ يقرؤها السيو، والتذييل، وإتمامُ الطلب،
     * وأقسامُ الصفحة. أربعةٌ وخمسون استعلامًا لصفحةٍ عامّةٍ يفتحها كلُّ زبون.
     *
     * والقيمُ لا تتبدّل داخل الطلب إلّا بكتابةٍ — وكلُّ كتابةٍ تُبطلها
     * (انظر `Setting::booted`). والذاكرةُ تموت مع الطلب فلا تُقرأ قيمةٌ
     * قديمة في طلبٍ تالٍ.
     *
     * @var array<string, array<string, string>>
     */
    private static array $memo = [];

    /**
     * تراكبُ المعاينة — انظر `overlay`.
     *
     * ويُنسى مع الذاكرة في `forget`: الاختبارُ والعاملُ يتشاركان العمليّة،
     * وتراكبُ معاينةٍ يبقى بعد طلبه يُري متجرًا إعداداتِ غيره.
     *
     * @var array<string, array<string, string>>
     */
    private static array $overlay = [];

    /**
     * قيمٌ تُقرأ بدل المحفوظ في هذا الطلب وحدَه — للمعاينة لا غير.
     *
     * ═══ لمَ هنا، ولمَ في الذاكرة لا في القاعدة ═══
     *
     * صاحبُ المتجر يكتب عنوانًا في المحرّر ويريد أن يراه كما يراه زبونُه
     * **قبل أن يحفظ**. والطريقُ الوحيد لِأن يكون ما يراه صادقًا هو أن
     * يُصيَّر القالبُ نفسُه بحروفه وCSSه — لا رسمٌ يشبهه: عارضٌ ثانٍ يُكتب
     * بيدٍ أخرى يقول غيرَ ما يقوله القالب، فيرتّب صفحةً ويُنشر غيرُها.
     *
     * وكلُّ ما يرسم الواجهةَ الخاصّة يمرّ بـ`group` — `StorePage`
     * و`StoreNav` و`StoreSeo` و`MerchantData` و`WebCheckout` كلُّها تقرأ
     * منها. فالتراكبُ هنا يبلغ الصفحةَ كلَّها بخيطٍ واحد، ولا يُنثر في
     * عشرة مواضع يُنسى أحدُها.
     *
     * ولا صفَّ يُكتب ولا ملفَّ: التراكبُ يعيش في الذاكرة ويموت بنهاية
     * الطلب. فالموقعُ المنشور لا يمسّه شيء، ولا يبقى بعد المعاينة أثرٌ
     * يُنشر سهوًا، ولا حاجةَ إلى كنسٍ ولا إلى انتهاء صلاحية.
     *
     * ═══ وما لا يمرّ ═══
     *
     * ما ليس من مفاتيح المجموعة يُطرح — بالقائمة نفسِها التي يُصفّي بها
     * `save`. فبابُ المعاينة لا يقبل ما لا يقبله بابُ الحفظ.
     *
     * ═══ ولمَ لا يُوضع التراكبُ ثمّ يُصيَّر ═══
     *
     * لأنّه ثابتٌ يعيش أطولَ من الطلب حيث تُعاد العمليّة: عاملُ الطابور،
     * وOctane، والاختبارات. وقد وقع فعلًا وكشفه حارسُ هذا الملفّ: عُوينت
     * مسوّدةٌ ثمّ فُتح الموقعُ العامّ في الطلب التالي **فخرجت المسوّدةُ
     * للزبون** — وهو أسوأُ ما يمكن أن يفعله بابُ معاينة.
     *
     * فلا بابَ يضع تراكبًا: يُمرَّر ما يُصيَّر، ويُنزَع في `finally` مهما
     * وقع. والنسيانُ **مستحيلٌ** لا مستبعَد — لا سطرَ يُكتب ليُنظَّف فيُنسى.
     *
     * @param  array<string, mixed>  $values
     * @template T
     * @param  callable(): T  $render
     * @return T
     */
    public static function withOverlay(int $businessId, string $group, array $values, callable $render)
    {
        $allowed = self::GROUPS[$group] ?? [];
        $clean = [];

        foreach ($values as $key => $value) {
            if (! array_key_exists($key, $allowed) || ! is_scalar($value)) {
                continue;
            }

            $clean[$key] = is_bool($value) ? ($value ? '1' : '0') : (string) $value;
        }

        if ($clean === []) {
            return $render();
        }

        $slot = $businessId.':'.$group;
        $had = self::$memo[$slot] ?? null;

        self::$overlay[$slot] = $clean;
        unset(self::$memo[$slot]);

        try {
            return $render();
        } finally {
            unset(self::$overlay[$slot], self::$memo[$slot]);

            // وما كان محفوظًا في الذاكرة قبل التراكب يعود — فلا يُدفع ثمنُ المعاينة استعلامًا
            if ($had !== null) {
                self::$memo[$slot] = $had;
            }
        }
    }

    /** يُنادى من `Setting` عند كلّ كتابةٍ أو حذف */
    public static function forget(?int $businessId = null): void
    {
        if ($businessId === null) {
            self::$memo = [];
            self::$overlay = [];

            return;
        }

        foreach ([...array_keys(self::$memo), ...array_keys(self::$overlay)] as $key) {
            if (str_starts_with($key, $businessId.':')) {
                unset(self::$memo[$key], self::$overlay[$key]);
            }
        }
    }

    /** قيم مجموعةٍ كما هي محفوظة — والناقص يعود بافتراضيّه */
    public static function group(int $businessId, string $group): array
    {
        if (isset(self::$memo[$businessId.':'.$group])) {
            return self::$memo[$businessId.':'.$group];
        }

        $defaults = self::GROUPS[$group] ?? [];
        $saved = Setting::where('business_id', $businessId)
            ->whereIn('key', array_keys($defaults))->pluck('value', 'key')->all();

        $out = [];
        foreach ($defaults as $key => $default) {
            // القيمة المحفوظة فارغةً قصدٌ لا غياب: لا تُستبدل بالافتراضيّ
            $out[$key] = array_key_exists($key, $saved) && $saved[$key] !== null ? $saved[$key] : $default;
        }

        /*
         * والمعاينةُ فوق المحفوظ — انظر `overlay`.
         *
         * وموضعُه بعد الافتراضيّات لا قبلها: المعاين يرى ما كتب، وما لم
         * يكتبه يبقى على حاله المحفوظ — لا يعود إلى فراغٍ لم يطلبه.
         */
        $out = array_merge($out, self::$overlay[$businessId.':'.$group] ?? []);

        return self::$memo[$businessId.':'.$group] = $out;
    }

    /** يحفظ ما يخصّ المجموعة من المُدخَل، ويتجاهل ما ليس منها */
    public static function save(int $businessId, string $group, array $values): void
    {
        $allowed = array_keys(self::GROUPS[$group] ?? []);

        foreach ($values as $key => $value) {
            if (! in_array($key, $allowed, true)) {
                continue;
            }

            Setting::updateOrCreate(
                ['business_id' => $businessId, 'key' => $key],
                ['value' => is_bool($value) ? ($value ? '1' : '0') : (string) ($value ?? '')],
            );
        }
    }
}
