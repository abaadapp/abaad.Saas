<?php

namespace App\Support\Store;

/**
 * نُشرت مسوّدةٌ غيرُ التي رآها الناشر.
 *
 * موظّفان يفتحان المحرّر، يكتب أحدُهما ويحفظ، ثمّ يضغط الآخرُ «نشر» وعلى
 * شاشته ما كان قبل ساعة. فيُنشر ما لم يره، ويُلام على نصٍّ لم يكتبه.
 *
 * ولا يُحسم بالتوقيت ولا بآخر من كتب: يُقال له إنّ المسوّدة تبدّلت، وتُحمَّل
 * له من جديد — فالقرارُ قرارُه بعد أن يرى.
 */
class StaleDraft extends \RuntimeException
{
    public function __construct()
    {
        parent::__construct('تبدّلت المسوّدة بعد أن فتحت الشاشة');
    }
}
