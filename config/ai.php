<?php

/*
 * المساعدُ الذكيّ — لا سرَّ في هذا الملفّ.
 *
 * المفتاحُ من البيئة: في ملفّ الخادم لا في المستودع ولا في قاعدة البيانات.
 * وقاعدةُ البيانات مقصودةٌ بالذكر — ما يُخزَّن فيها يخرج مع كلّ نسخةٍ
 * احتياطية، ونسخُ القاعدة تُنقل وتُشارك.
 *
 * ═══ ومطفأٌ افتراضًا ═══
 *
 * بلا مفتاحٍ لا يعمل شيء، وتقولُ الشاشةُ ذلك ولا تدّعي غيرَه. ونداءٌ إلى
 * مزوّدٍ بلا مفتاح يُردّ بعد ثوانٍ برسالةٍ إنجليزيّةٍ لا يفهمها التاجر.
 */
return [
    /*
     * المزوّد: `anthropic` أو `null`.
     *
     * و`null` ليست عطلًا بل حالٌ صريحة: «لا مزوّد» — فتُعرض الإشاراتُ
     * المقيسة وحدَها، ولا زرَّ اقتراحٍ يُعرض ولا يُفتح.
     */
    'provider' => env('AI_PROVIDER', 'null'),

    'anthropic' => [
        'key' => env('ANTHROPIC_API_KEY'),

        'url' => env('ANTHROPIC_URL', 'https://api.anthropic.com/v1/messages'),

        /*
         * ونسخةُ الواجهة إعدادٌ لا ثابتٌ في الكود: تُصدَر نسخةٌ وتُوقف
         * القديمة، فترقيتُها يجب أن تكون سطرًا في البيئة لا نشرةَ كودٍ كاملة.
         */
        'version' => env('ANTHROPIC_VERSION', '2023-06-01'),

        'model' => env('ANTHROPIC_MODEL', 'claude-sonnet-5'),
    ],

    /*
     * سقفُ ما يُولَّد.
     *
     * ردُّ مبيعاتٍ على واتساب سطران أو ثلاثة. وسقفٌ مرتفعٌ يعني ردًّا طويلًا
     * لا يُقرأ على الهاتف — ويعني فاتورةً أعلى بلا مقابل.
     */
    'max_tokens' => (int) env('AI_MAX_TOKENS', 600),

    /*
     * مهلةُ النداء.
     *
     * وموظّفُ المبيعات ينتظرها أمام الشاشة: خمسٌ وأربعون ثانيةً على اقتراحٍ
     * ليست انتظارًا بل عطل. والفشلُ الصريح خيرٌ من دوّارةٍ لا تقف.
     */
    'timeout' => (int) env('AI_TIMEOUT', 20),

    /*
     * أقصى ما يُرسَل من المحادثة إلى النموذج.
     *
     * ولا تُرسل المحادثةُ كلُّها: هي بياناتُ شخصٍ، وأقلُّ ما يكفي هو ما
     * يُرسَل. وعشرون رسالةً تُغطّي سياقَ بيعٍ كاملًا.
     */
    'context_messages' => (int) env('AI_CONTEXT_MESSAGES', 20),
];
