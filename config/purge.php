<?php

return [

    /*
    |--------------------------------------------------------------------------
    | الحذفُ النهائيُّ للشركة
    |--------------------------------------------------------------------------
    |
    | مفتاحٌ مغلقٌ افتراضيًّا. الكودُ يُنشر ولا يعمل حتّى يُفتح صراحةً على
    | الخادم — فلا زرَّ يُعرض، ولا مسارَ يُستجاب له، ولو عرف أحدٌ عنوانَه.
    |
    | وسببُ المفتاح أنّ هذا الفعلَ لا تراجعَ فيه: بياناتُ تاجرٍ تُمحى ومعها
    | ملفّاتُه. فبابُه يُفتح بقرارٍ على الخادم لا بنشرةِ كود.
    |
    */

    'enabled' => env('BUSINESS_PURGE_ENABLED', false),

];
