<?php

/*
 * استعادة الحساب — أرقامٌ تُضبط ولا تُكتب في الكود.
 *
 * ولا سرَّ في هذا الملفّ: اعتمادات البريد في `config/mail.php` وقيمُها في
 * ملفّ الخادم، ولا تمرّ من هنا.
 */
return [
    /** عمر الرمز — عشر دقائق تكفي لفتح البريد ونسخ ستّة أرقام */
    'otp_ttl' => (int) env('PASSWORD_RECOVERY_OTP_TTL', 600),

    /**
     * محاولاتٌ خاطئة قبل إبطال الرمز.
     *
     * ستّة أرقامٍ مليونُ احتمال؛ خمس محاولاتٍ لا تقترب منها. وبلا حدٍّ يُجرَّب
     * المليون كلّه في ساعات.
     */
    'otp_max_attempts' => (int) env('PASSWORD_RECOVERY_OTP_MAX_ATTEMPTS', 5),

    /** أقلّ ما بين إرسالين — بالخادم لا بالعدّاد في الشاشة */
    'resend_cooldown' => (int) env('PASSWORD_RECOVERY_RESEND_COOLDOWN', 60),

    /** عمر المحاولة كلّها — من أوّل طلبٍ إلى آخر خطوة */
    'challenge_ttl' => (int) env('PASSWORD_RECOVERY_CHALLENGE_TTL', 1800),

    /**
     * مهلة تعيين كلمة المرور بعد اجتياز الرمز.
     *
     * ربع ساعة: تكفي لكتابة كلمةٍ وتأكيدها، ولا تترك إذنًا مفتوحًا على
     * جهازٍ تُرك في مقهى.
     */
    'authorization_ttl' => (int) env('PASSWORD_RECOVERY_AUTH_TTL', 900),
];
