<?php

return [

    /*
     * النطاق الذي تُبنى عليه عناوين المتاجر: «متجري.abaadapp.om».
     *
     * ويلزمه على الخادم شيئان يُضبطان مرّةً: سجلّ DNS بالحرف البدل
     * (‏`*.abaadapp.om`‎) وشهادة SSL بالحرف البدل. وقبل ضبطهما يعمل العنوان
     * البديل `/s/{slug}` — انظر `Storefront::fallbackUrl`.
     */
    'domain' => env('STOREFRONT_DOMAIN', 'abaadapp.om'),

    /*
     * وهل يخدمها الخادمُ **فعلًا**؟ — سؤالٌ غير «ما هو النطاق».
     *
     * `canonical` يقول لمحرّك البحث «الأصلُ هنا فافهرسه». فلو أشار إلى
     * `متجري.abaadapp.om` قبل أن يوجد سجلُّ DNS بالحرف البدل وشهادةٌ مثله،
     * لَدلَّ المحرّكَ على عنوانٍ لا يُحلّ — وترك الصفحةَ الحيّة بلا فهرسة.
     * وهو أسوأ من ألّا يكون هناك `canonical` أصلًا.
     *
     * فيبقى `false` حتى يُضبط الاثنان على الخادم، ثمّ يُرفع بمتغيّرٍ واحد.
     * انظر `Storefront::canonical` و`Storefront/docs/DEPLOY.md`.
     */
    'subdomains' => (bool) env('STOREFRONT_SUBDOMAINS', false),

    /*
     * العنوانُ الذي يوجّه إليه التاجرُ نطاقَه — `CNAME` واحد.
     *
     * ولا يُعطى عنوانُ الخادم بالأرقام: تبديلُ خادمٍ يومًا يعني مراسلةَ كلّ
     * تاجرٍ ليعدّل سجلَّه. واسمٌ وسيط يُبدَّل مرّةً عندنا.
     */
    'connect' => env('STOREFRONT_CONNECT', 'connect.'.env('STOREFRONT_DOMAIN', 'abaadapp.om')),

    /*
     * وعنوانُ الخادم بالأرقام — للنطاق العاري وحده.
     *
     * `myshop.om` بلا `www` لا يقبل فيه أكثرُ المسجّلين `CNAME`، فيلزمه
     * `A`. ويبقى فارغًا حتى يُقرَّر عنوانٌ ثابت — والشاشة حينها تعرض
     * `www` بدلًا منه، وهو يعمل عند الجميع.
     */
    'connect_ip' => env('STOREFRONT_CONNECT_IP', ''),

    /*
     * وهل يخدم هذا الخادمُ نطاقاتِ التجّار؟
     *
     * ربطُ النطاق في اللوحة يعمل بلا هذا: يُحفظ الصفّ، ويُتحقَّق من التوجيه،
     * وتُقرأ الحال. لكنّ **خدمةَ** الصفحة على نطاق التاجر تلزمها كتلةُ
     * nginx تلتقط المضيف المجهول وشهادةٌ تُصدَر له. فيبقى `false` حتى
     * يُضبطا، ثمّ يُرفع بمتغيّرٍ واحد.
     *
     * ولا يُرفع قبلهما: المسارُ يلتقط كلَّ مضيفٍ ليس لنا، وبلا شهادةٍ يصل
     * الزائرُ إلى تحذيرِ أمانٍ باسم متجر التاجر.
     */
    'custom_domains' => (bool) env('STOREFRONT_CUSTOM_DOMAINS', false),

    /*
     * سعرُ العنوان — من موضعٍ واحد يُقرأ في الشاشة وفي كلّ ما يُبنى عليه.
     *
     * وصفرٌ ليس «سعرًا مفقودًا» بل سعرٌ معلوم: تعرضه الشاشة «مشمول في باقتك
     * بلا رسوم». فمن لم يقرّر بعدُ لا يظهر لتاجره رقمٌ مخترع، ومن قرّر يكتب
     * الرقم هنا فيظهر في مكانٍ واحد لا في ثلاثة نصوصٍ متفرّقة.
     *
     * والعرضُ وحده: لا يُنشئ فاتورةً ولا يخصم شيئًا. فوترةُ الاشتراكات تقع
     * في لوحة المنصّة، وزرٌّ يقول «ادفع» ولا يدفع أسوأ من سعرٍ مكتوبٍ بجانبه
     * «يُضاف إلى فاتورتك».
     */
    'pricing' => [
        // نطاق أبعاد الفرعيّ: متجري.abaadapp.om
        'subdomain' => [
            'monthly' => env('STOREFRONT_SUBDOMAIN_MONTHLY', 0),
            'yearly' => env('STOREFRONT_SUBDOMAIN_YEARLY', 0),
        ],
    ],

    /*
     * العملة التي تُكتب بها هذه الأسعار.
     *
     * وهي عملةُ المنصّة لا عملةَ متجرٍ بعينه: التاجر قد يبيع بالدرهم ويشترك
     * بالريال، وقراءةُ رمزِ متجره هنا تكتب سعرَ الاشتراك بعملةٍ لا يُدفع بها.
     */
    'currency' => env('STOREFRONT_CURRENCY', 'ر.ع'),

];
