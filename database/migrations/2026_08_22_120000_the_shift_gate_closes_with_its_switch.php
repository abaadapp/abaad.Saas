<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Support\Facades\DB;

/**
 * يُطفئ `require_open_shift` بعد أن زال المقبض الذي يُطفئه.
 *
 * القسم «وردية الصندوق» أُزيل من الإعدادات بطلب صاحب النظام. ومفتاحه لم يكن
 * زينةً: `Shifts` يقرؤه فيمنع البيع ما لم تُفتح وردية. فلو تُرك مرفوعًا على
 * متجرٍ رفعه، لوقف صندوقُه ولا شاشة يُطفأ منها — والكاشير يرى المنع ولا يجد
 * له سببًا، وصاحبُ المتجر يبحث عن مفتاحٍ حُذف.
 *
 * والإطفاء لا الترك: منعُ البيع أخطر تصرّفٍ في نقطة بيع، وتركُه بلا مفتاح
 * يجعل العطل يظهر عند أوّل بيعةٍ صباحًا لا عند فتح الإعدادات.
 *
 * أمّا `shift_max_hours` فيبقى عاملًا على قيمته: يُقفل الوردية المنسيّة تلقائيًّا،
 * ولا يمنع أحدًا من شيء، فغيابُ شاشته لا يحبس أحدًا.
 */
return new class extends Migration
{
    public function up(): void
    {
        DB::table('settings')->where('key', 'require_open_shift')->update(['value' => '0']);
    }

    /**
     * لا رجعة: القيمة السابقة تمنع البيع، وإعادتُها بلا الشاشة التي تُطفئها
     * تُعيد الحبس نفسه.
     */
    public function down(): void {}
};
