<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Support\Facades\DB;

/**
 * الحسابُ الذي يرحّل إليه النظام يعود ورقةً — لا فرعًا فوق فروع.
 *
 * ═══ ما وقع ═══
 *
 * قائمةُ «الحساب الأب» في شجرة الحسابات كانت تعرض الحسابات كلَّها، ومنها ما
 * يقصده الترحيلُ التلقائيُّ بمفتاحه. فأضاف تاجرٌ «بترول (تنقل)» تحت «٥٩٠٠
 * مصروفات أخرى» — تبويبٌ معقول تمامًا — فصار ٥٩٠٠ أبًا.
 *
 * و`Account::isPostable` تشترط ألّا فروعَ للحساب: مجموعُ الأب هو مجموعُ
 * فروعه، وقيدٌ عليه مباشرةً يجعل الشجرة تقول رقمين. فتوقّف كلُّ ما يُرحَّل
 * إلى ٥٩٠٠: تسويةُ المخزون، وفاقدُ الجرد، والقيدُ اليدويُّ عليه (شاشةُ
 * القيود تُسقط الآباء من قائمتها أصلًا). ولا رسالةَ تقول لماذا — يضغط
 * التاجر «حفظ التسوية» فيُردّ بخطأ خادم.
 *
 * ═══ وما تفعله هذه ═══
 *
 * ترفع كلَّ فرعٍ وُضع تحت حسابٍ نظاميّ فتجعله **أختًا** له تحت جدِّه —
 * فيعود النظاميُّ ورقةً تقبل القيد، ويبقى الفرعُ في مكانه من الشجرة بالاسم
 * والرمز والرصيد.
 *
 * وهو ما تفعله أوراقُ الحسابات البنكية أصلًا: ورقةُ كلّ بنكٍ تُوضع أختًا
 * للحساب النظاميّ لا ابنةً له.
 *
 * ولا يتحرّك سطرٌ واحدٌ من الدفتر: `parent_id` وحده يتغيّر. والأرصدةُ تُقرأ
 * كما كانت، لأنّ `Account::balance` تعدّ سطورَ الحساب نفسِه لا سطورَ فروعه.
 *
 * ═══ والبابُ أُغلق فوق ذلك ═══
 *
 * `ChartController` صارت تردّ الأبوّة على حسابٍ نظاميّ، والشاشةُ لا تعرضه في
 * قائمة الآباء. وهذه لِما وقع قبل الإغلاق.
 *
 * و`down` فارغةٌ عمدًا: إعادةُ الفرع تحت النظاميّ إعادةٌ للعطب نفسِه.
 */
return new class extends Migration
{
    public function up(): void
    {
        $systems = DB::table('accounts')->whereNotNull('system_key')->get(['id', 'business_id', 'parent_id']);

        foreach ($systems as $system) {
            DB::table('accounts')
                ->where('business_id', $system->business_id)
                ->where('parent_id', $system->id)
                ->update(['parent_id' => $system->parent_id]);
        }
    }

    public function down(): void
    {
        // ‏لا رجوع: إعادةُ الفرع تحت الحساب النظاميّ تُوقف الترحيل إليه من جديد
    }
};
