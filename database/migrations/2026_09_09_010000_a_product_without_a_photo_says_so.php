<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Support\Facades\DB;

/**
 * صورةٌ لم يرفعها أحد تُرفع من عمود المنتج.
 *
 * ═══ ما وقع ═══
 *
 * `ProductController::store` كانت تكتب في عمود الصورة رابط `picsum.photos`
 * — خدمةَ صورٍ عشوائيّة على الإنترنت — لكلّ منتجٍ يُضاف بلا صورة. فيصير
 * المنتجُ في نظر النظام **مصوَّرًا**: `ProductImages::hasRealMain` تقرأ
 * العمود الخام فتقول «نعم»، فيُنشر بتلك الصورة في متجر التاجر على الإنترنت،
 * وتُعدّ في سقف الصور، ويُرسم لها زرُّ حذفٍ لملفٍّ لا وجود له.
 *
 * ووقع على متجرٍ حقيقيّ: صنفٌ اسمه «لانيارد» يحمل صورةَ شيءٍ لا صلة له به.
 *
 * ═══ وما تفعله هذه ═══
 *
 * تُفرغ العمود من كلّ رابطٍ إلى تلك الخدمة. ولا يُحذف ملفّ: هذه ليست ملفّات
 * — لا شيء منها على قرص الخادم أصلًا. والمنتجُ بعدها يقول «بلا صورة»، وهي
 * الحالةُ التي كتبتها الشاشات كلُّها ولم تقع مرّةً.
 *
 * ولا تمسّ صورةً رفعها التاجر: الشرطُ على نصّ الرابط لا على غيابه.
 *
 * و`down` فارغةٌ عمدًا: إعادةُ صورةٍ عشوائيّة إلى منتجٍ إعادةٌ للعطب.
 */
return new class extends Migration
{
    public function up(): void
    {
        DB::table('products')->where('image', 'like', '%picsum.photos%')->update(['image' => null]);
    }

    public function down(): void
    {
        // ‏لا رجوع: صورةٌ لم يرفعها أحد لا تُعاد إلى منتج
    }
};
