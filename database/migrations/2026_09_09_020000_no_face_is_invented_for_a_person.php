<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Support\Facades\DB;

/**
 * وجهٌ لم يرفعه صاحبُه يُرفع من عمود المستخدم.
 *
 * ═══ ما وقع ═══
 *
 * «إضافة موظّف» و«إضافة مستخدم للمنصّة» كانتا تكتبان في `users.avatar` رابط
 * `picsum.photos` — خدمةَ صورٍ عشوائيّة على الإنترنت. فيُولد الحسابُ ووجهُ
 * إنسانٍ آخر في ملفّه، وفي شارته على شاشة الإعدادات، وفي شاشة اختيار
 * الكاشير التي يضغط عليها الموظّف ليبدأ ورديته.
 *
 * ولا يفرّق أحدٌ بينه وبين صورةٍ رُفعت: العمود واحد، والشاشةُ تعرض ما فيه.
 * ومن أراد صورتَه الحقيقيّة رفعها من ملفّه الشخصيّ، فلم يعرف أنّ ما يستبدله
 * لم يضعه هو.
 *
 * ووقع على متجرٍ حقيقيّ: موظّفان يحملان وجهين لا يعرفهما صاحب المحلّ.
 *
 * ═══ وما تفعله هذه ═══
 *
 * تُفرغ العمود من كلّ رابطٍ إلى تلك الخدمة. ولا ملفّ يُحذف — لا شيء منها على
 * قرص الخادم. والحسابُ بعدها يقول «بلا صورة»، فيُرسم الحرفُ الأول من اسمه:
 * وهي الحالةُ التي كتبتها الشاشات كلُّها ولم تقع مرّةً.
 *
 * وصورةٌ رفعها صاحبُها لا تُمسّ: الشرطُ على نصّ الرابط لا على غيابه.
 *
 * والمتجرُ التجريبيّ يبقى بوجوهه — هي بيانُ عرضٍ يُبذَر صراحةً في `SeedData`،
 * لا بيانُ تاجر. وهو الحدُّ نفسه الذي رُسم لصور المنتجات.
 *
 * و`down` فارغةٌ عمدًا: إعادةُ وجهٍ عشوائيّ إلى إنسانٍ إعادةٌ للعطب.
 */
return new class extends Migration
{
    public function up(): void
    {
        $demo = DB::table('businesses')->where('is_demo', true)->pluck('id')->all();

        /*
         * و«ليس من التجريبيّ» تشمل من لا نشاط له.
         *
         * `whereNotIn` وحدها تُسقط صفوف `business_id IS NULL` — لأنّ
         * `null not in (…)` ليست صحيحةً في SQL بل مجهولة. ومديرُ المنصّة
         * بلا نشاطٍ مربوط، فكان وجهُه المخترع سينجو وحده من التنظيف.
         */
        DB::table('users')
            ->where('avatar', 'like', '%picsum.photos%')
            ->when($demo !== [], fn ($q) => $q->where(
                fn ($w) => $w->whereNull('business_id')->orWhereNotIn('business_id', $demo)
            ))
            ->update(['avatar' => null]);
    }

    public function down(): void
    {
        // ‏لا رجوع: وجهٌ لم يرفعه صاحبُه لا يُعاد إليه
    }
};
