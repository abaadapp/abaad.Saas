<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

/**
 * بابٌ يكتب منه الزبونُ رأيَه بيده — ولم يكن له باب.
 *
 * ═══ ما كان ═══
 *
 * في النظام شاشةُ «تقييمات العملاء»: تُنشَر وتُرفَض ويُردّ عليها، وتخرج على
 * موقع التاجر. وطريقُ دخولها **واحد**: أن يكتبها التاجر بنفسه من زرّ «تسجيل
 * تقييم». فمتجرٌ لم يفتح تلك الشاشة يبقى عنده صفرُ تقييماتٍ إلى الأبد —
 * لا لأنّ زبائنه صامتون، بل لأنّ لا أحدَ يستطيع الكتابة.
 *
 * وشاشةٌ تُدير ما لا يصل ليست شاشةً تُدير شيئًا.
 *
 * ═══ ولمَ رمزٌ لطلبٍ بعينه لا نموذجٌ مفتوح ═══
 *
 * نموذجٌ على صفحة المتجر يكتب فيه من يشاء ما يشاء يجعل واجهةَ التاجر بابًا
 * لكلّ رسالةٍ يكتبها أيّ أحد — ومنافسٌ وروبوتٌ أوّلُ من يجده. والتاجر يُغرَق
 * في «معلّق» فيكفّ عن قراءته، فيمرّ الحقيقيُّ مع الزائف.
 *
 * والرمزُ يقول ما لا يقوله النموذج: **هذا اشترى**. يُبنى لطلبٍ بلغ يدَ
 * صاحبه، ويُرسله التاجرُ إلى رقمِ ذلك الطلب وحدَه.
 *
 * ═══ ورأيٌ واحدٌ لكلّ طلب — بقيدٍ في القاعدة ═══
 *
 * زبونٌ يفتح الرابط مرّتين — أو يضغط «إرسال» ضغطتين على شبكةٍ بطيئة — يكتب
 * صفَّين. والمعدّلُ يُحسب على الصفوف، فرأيٌ واحدٌ يُحسب مرّتين.
 *
 * والقيدُ في القاعدة لا فحصٌ في المتحكّم: طلبان متزامنان يمرّان معًا على
 * «هل كُتب؟» فيجيب كلاهما «لا». و`order_id` يبقى فارغًا لما يُسجّله التاجر
 * بيده — والفراغُ لا يصطدم بالفراغ في الفهرس الفريد على المحرّكين معًا.
 */
return new class extends Migration
{
    public function up(): void
    {
        Schema::table('orders', function (Blueprint $table) {
            /*
             * الرمزُ اثنان وعشرون حرفًا — كرمز الورقة، ولسببه نفسِه.
             *
             * يُمسح من رسالةِ واتساب أو يُطبع أسفل الإيصال، وكلُّ حرفٍ فيه
             * يزيد كثافةَ الرمز المطبوع. وستّةٌ وستّون احتمالًا في اثنين
             * وعشرين موضعًا لا تُخمَّن ولا تُعَدّ.
             */
            $table->string('review_token', 24)->nullable()->unique()->after('review_request_sent_at');
        });

        Schema::table('reviews', function (Blueprint $table) {
            $table->unique('order_id');
        });
    }

    public function down(): void
    {
        Schema::table('reviews', function (Blueprint $table) {
            $table->dropUnique(['order_id']);
        });

        Schema::table('orders', function (Blueprint $table) {
            $table->dropUnique(['review_token']);
            $table->dropColumn('review_token');
        });
    }
};
