<?php

use App\Models\JobTitle;
use Illuminate\Database\Migrations\Migration;
use Illuminate\Support\Facades\DB;

/**
 * كلُّ متجرٍ يملك الستَّ — ولا مسمًّى يصنع صاحبَ نشاطٍ ثانيًا.
 *
 * ═══ العطب ═══
 *
 * الوظائفُ الستُّ بُذرت مرّةً واحدة في هجرةِ ٢٠٢٦-٠٧-٢١ لمن كان قائمًا
 * يومَها. ومن جاء بعدَها لم يرثها: `MerchantAccount::provision` كانت تبذر
 * **واحدةً** — «مدير» دورُها `admin`.
 *
 * فيفتح التاجرُ الجديد «الرواتب والموظفين» فلا يجد مسمًّى لكاشيرٍ ولا لبائعٍ
 * ولا لمحاسب. والوحيدُ الجاهز يصنع **صاحبَ نشاطٍ ثانيًا**: من أُسندت إليه
 * صار دورُه `admin` — يملك كلَّ قسم، ويمسّ كلَّ حساب، ويعيد تعيين كلمة مرور
 * المالك. وعلى الإنتاج متجرٌ كهذا: «تيست 1» بوظيفةٍ واحدة دورُها `admin`.
 *
 * ═══ وهذه تُصلح ما كُتب ═══
 *
 * والمصدرُ صار واحدًا (`JobTitle::seedDefaults`) فلا يتكرّر — لكنّ ما كُتب
 * قبلَه لا يُصلحه مصدر.
 */
return new class extends Migration
{
    public function up(): void
    {
        /*
         * أوّلًا: مسمًّى دورُه `admin` يصير `manager`.
         *
         * `Roles::STAFF` تُخرج `admin` من الأدوار التي تُسنَد — «وظيفةٌ تمنحه
         * لأيّ موظّف تمنحه كلَّ شيء». فما كُتب منها قبل ذلك يُنزل إلى أعلى
         * دورٍ يُسنَد: `manager`، وهو ما تحمله «مدير» في المتجرين الأقدمين.
         *
         * ولا يمسّ ذلك دورَ من يحملها اليوم: صفُّ صاحب النشاط `admin`، ولا
         * تكتبه وظيفةٌ بعد اليوم — انظر `EmployeeController::update`.
         */
        DB::table('job_titles')->where('role', 'admin')->update(['role' => 'manager']);

        // ثانيًا: ما نقص من الستّ يُبذَر — والتغطيةُ بالدور لا بالاسم
        foreach (DB::table('businesses')->pluck('id') as $bid) {
            JobTitle::seedDefaults((int) $bid);
        }
    }

    /**
     * ولا رجعة: مسمًّى بُذر قد يحمله موظّفٌ الآن، ومحوُه يقطعه عن وظيفته.
     * ورفعُ `manager` إلى `admin` ثانيةً يُعيد بابًا أُغلق عمدًا.
     */
    public function down(): void {}
};
