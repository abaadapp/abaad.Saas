<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Support\Facades\DB;

/**
 * كلُّ حركةٍ ماليّة تسمّي البابَ الذي دخلت منه بيعتُها.
 *
 * ═══ العطبُ الذي تعالجه ═══
 *
 * هجرةُ `kind` كتبت النوعَ لكلّ صفٍّ **قائمٍ يومَها** ثمّ مضت — ولم يكتبه
 * مَن يُنشئ الحركةَ بعدها. فنقطةُ البيع تكتب صفَّ الدخل بلا نوع، والموقعُ
 * كذلك. والنتيجةُ أنّ كلّ بيعةٍ وقعت **بعد** تلك الهجرة تُقرأ في «الحركة
 * المالية» باسم «حركة» لا «مبيعات»، ولا يبلغها مُرشِّحُ النوع، ولا يعدّها
 * `Transaction::scopeSales`.
 *
 * ولم يُكتشف بالنظر: الشاشة تعرض الصفَّ ومبلغَه وتاريخَه صحيحةً كلَّها،
 * وعمودٌ واحد فيه كلمةٌ عامّة. والمجاميع تُحسب من `type` لا من `kind`،
 * فالأرقام صادقة — والتصنيف وحده هو الذي ضاع.
 *
 * والمصدرُ عولج في موضعه (`PosController` و`WebCheckout`)، وهذه تستدرك ما
 * كُتب بينهما: تقرأ قناةَ الطلب — لا تخمّن — فتكتب لكلّ حركةٍ نوعَها.
 *
 * وما لا طلبَ له لا يُمسّ: مصروفٌ أو تحويلٌ بلا `kind` شأنٌ آخر لا تدّعي
 * هذه الهجرةُ معرفتَه.
 */
return new class extends Migration
{
    public function up(): void
    {
        $sales = DB::table('transactions')->whereNull('kind')->whereNotNull('order_id');

        /*
         * والقناةُ تُقرأ من الطلب نفسِه بجدولٍ مضموم.
         *
         * فطلبُ موقعٍ يأخذ `web_sale`، وما عداه — صندوقًا كان أو طلبًا سبق
         * العمودَ فقناتُه فارغة — يأخذ `sale`: هو ما كانت تكتبه الهجرةُ
         * الأولى لكلّ ما له طلب، فلا تُغيَّر قراءةُ تاريخٍ قائم.
         */
        (clone $sales)->whereIn('order_id', fn ($q) => $q->select('id')->from('orders')
            ->where('channel', 'website'))->update(['kind' => 'web_sale']);

        (clone $sales)->update(['kind' => 'sale']);
    }

    /**
     * ولا رجعةَ لها.
     *
     * محوُ النوع يعيد العطبَ نفسَه، ولا يستعيد شيئًا: العمودُ كان فارغًا
     * بالخطأ لا بالقصد. والهجرةُ التي أنشأت العمود تحذفه كلَّه إن أُريد.
     */
    public function down(): void {}
};
