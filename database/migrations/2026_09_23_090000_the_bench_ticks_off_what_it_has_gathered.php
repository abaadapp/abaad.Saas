<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

/**
 * علاماتُ التجهيز — ما جُمع من الطلب على الطاولة.
 *
 * ═══ لمَ جدولٌ ولا حقلٌ في الطلب ═══
 *
 * العلامةُ تخصّ **بندًا** لا طلبًا: «الورد جُمع، والدبّ لم يُجلب بعد». وعمودُ
 * JSON في `orders` يعني قراءةَ الصفّ كلِّه وكتابتَه لأجل مربّعٍ واحد — وموظّفان
 * على طاولتين يكتبان معًا فيمحو آخرُهما علامةَ الأوّل بلا أن يعلم.
 *
 * ═══ ووجودُ الصفّ هو العلامة ═══
 *
 * لا عمود `checked` يُقلب: المؤشَّر له صفّ، وغيرُ المؤشَّر لا صفّ له. فلا حالةَ
 * ثالثة تُنسى («صفٌّ موجودٌ بقيمة false» ليست «لم يُؤشَّر» ولا «أُلغي تأشيره»)،
 * ورفعُ العلامة حذفٌ لا كتابةٌ ثانية.
 *
 * ═══ والمفتاحُ نصٌّ لا مفتاحٌ أجنبيّ إلى البند ═══
 *
 * ثلاثةُ أنواعٍ تُؤشَّر: بندٌ (`item:{id}`) وإضافةٌ (`addon:{id}`) ومهمّةٌ ثابتة
 * (`task:packaging`). ولو كان عمودًا أجنبيًّا لَاحتاج عمودين يتبادلان الفراغ
 * وقيدًا يمنع امتلاءهما معًا — لأجل ما يُقرأ دائمًا كنصّ.
 *
 * وهو ثابتٌ تاريخيًّا: معرّفاتُ البنود والإضافات لقطةُ البيع لا تتغيّر بعده،
 * فتبديلُ منتجٍ أو قالبٍ اليومَ لا يُزحزح علامةً وُضعت أمس.
 *
 * ═══ ولا يمسّ هذا الجدولُ مخزونًا ولا مالًا ولا حالةَ طلب ═══
 *
 * علامةٌ تشغيليّة تقول «جمعتُه»، لا حركةَ رفٍّ ولا قيدَ دفتر. وهي لا تنقل
 * الطلب: من يؤشّر كلَّ المربّعات يضغط «جاهز» بنفسه.
 */
return new class extends Migration
{
    public function up(): void
    {
        Schema::create('order_prep_checks', function (Blueprint $table) {
            $table->id();
            $table->unsignedBigInteger('business_id')->index();
            $table->unsignedBigInteger('order_id');
            /** `item:{id}` أو `addon:{id}` أو `task:{name}` — انظر `PrepChecklist` */
            $table->string('key', 64);
            /*
             * من وضعها ومتى — يُعرضان في التفاصيل.
             *
             * والاسمُ منسوخٌ مع المعرّف: حسابُ موظّفٍ يُحذف بعد سنة، ولا يجوز
             * أن يُمحى معه من الطاولة أنّ أحدًا جمع هذه الباقة.
             */
            $table->unsignedBigInteger('user_id')->nullable();
            $table->string('user_name')->nullable();
            $table->timestamps();

            /*
             * والمفتاحُ مرّةً واحدة للطلب الواحد.
             *
             * موظّفان يضغطان المربّعَ نفسه في اللحظة نفسها — وبلا هذا القيد
             * يُكتب صفّان، فيُرفع التأشير بضغطةٍ واحدة ويبقى الثاني: مربّعٌ
             * يعود مؤشَّرًا بعد رفعه، ولا شيء يقول لماذا.
             */
            $table->unique(['order_id', 'key']);
            // وقراءةُ اللوحة تسأل «علاماتُ هذه الطلبات» — مئتا طلبٍ في استعلام
            $table->index(['business_id', 'order_id']);
        });
    }

    public function down(): void
    {
        Schema::dropIfExists('order_prep_checks');
    }
};
