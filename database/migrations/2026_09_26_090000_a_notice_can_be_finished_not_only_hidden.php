<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

/**
 * ═══ حالةُ التنبيه — ولا جدولَ تنبيهاتٍ ثانيًا ═══
 *
 * تنبيهاتُ هذا النظام **مشتقّةٌ لا مخزَّنة**: `Demo::buildNotifications`
 * تحسبها من البيانات الحيّة في كلّ استطلاع، فلا صفَّ يُكتب حين تنفد بضاعةٌ
 * ولا صفَّ يُمحى حين تُعاد. وهذا ما يجعل «لا يُنبَّه مرّتين» مسألةَ بنيةٍ
 * لا مسألةَ حارس.
 *
 * فهذا الجدولُ لا يخزّن تنبيهًا — يخزّن **ما فعله المستخدمُ به**: أجّله،
 * أو أنجزه، أو فتحه ليقرأه. ولا صفَّ يُكتب إلّا عن فعلٍ وقع، فلا ينمو
 * بعدد ما مرّ في الجرس بل بعدد ما لُمس منه.
 *
 * ═══ ولمَ `cycle` ═══
 *
 * صنفٌ نفد فأُعيد تخزينُه ثمّ نفد ثانيةً مشكلةٌ **جديدة**، ومفتاحُها هو
 * المفتاحُ الأوّل نفسُه (`low-<المعرّف>`). فلو كان التفرّدُ على (المستخدم،
 * المفتاح) وحدَهما لَبقي إنجازُ المرّة الأولى مانعًا لتنبيه المرّة الثانية
 * إلى الأبد — وهو العطبُ الذي كانت `DISMISSAL_DAYS` تخفّفه بثلاثين يومًا
 * ولا تحلّه.
 *
 * فالدورةُ تُغلق حين يختفي المفتاحُ من البناء (`resolved_at`)، وعودتُه
 * تفتح دورةً برقمٍ تالٍ. والصفوفُ المغلقة تبقى — وهي سجلُّ «المكتملة».
 *
 * ولمَ ليس فهرسًا جزئيًّا على المفتوح وحدَه: القاعدتان تدعمانه بصيغتين
 * مختلفتين، والهجرةُ الواحدة تعمل على SQLite وPostgreSQL معًا. ورقمُ
 * الدورة يقول الشيءَ نفسَه بلا فرعٍ في الهجرة.
 */
return new class extends Migration
{
    public function up(): void
    {
        Schema::create('notification_states', function (Blueprint $table) {
            $table->id();
            $table->unsignedBigInteger('business_id');
            $table->unsignedBigInteger('user_id');
            $table->string('notif_key', 191);
            /* الدورةُ: كم مرّةً ظهرت هذه المشكلةُ بعينها ثمّ زالت */
            $table->unsignedInteger('cycle')->default(1);

            /*
             * لقطةُ نصّ التنبيه ورابطُه ساعةَ لُمس.
             *
             * التنبيهُ مشتقّ، فمتى زال سببُه لم يعد يُبنى — ولا سبيلَ إلى
             * استرجاع نصّه. وسجلُّ «المكتملة» بلا نصٍّ مفاتيحُ لا يقرؤها
             * أحد: «‏low-412 — أُنجز أمس» لا تقول شيئًا لصاحب المحلّ.
             *
             * وهما حقلان لا نسخةٌ من القسم الآخر: لا كمّيةَ ولا سعرَ ولا
             * حالةَ طلب — سطرٌ كما قرأه صاحبُه، ورابطٌ يعود به إلى السجلّ
             * الحقيقيّ حيث الأرقامُ حيّة.
             */
            $table->string('label', 191);
            $table->string('url', 500)->nullable();

            /* فُتح فقُرئ — والقراءةُ ليست إنجازًا */
            $table->timestamp('seen_at')->nullable();
            /* أُجّل إلى — وما بعده يعود إلى «تحتاج إجراء» إن بقي السبب */
            $table->timestamp('snooze_until')->nullable();
            /*
             * أُنجز متى — و«مَن» هو `user_id` نفسُه.
             *
             * الحالةُ شخصيّةٌ كالإخفاء من قبلها: لا يُنجز أحدٌ عن زميله فيُطفئ
             * جرسَه. وعمودُ «أنجزه فلان» يساوي `user_id` في كلّ صفّ، وعمودٌ
             * لا يختلف أبدًا يُوهم قارئَه أنّ خلفه سؤالًا.
             */
            $table->timestamp('done_at')->nullable();
            /* أُغلقت الدورة — بإنجازٍ أو بزوال السبب وحدَه */
            $table->timestamp('resolved_at')->nullable();

            $table->timestamps();

            /* دورةٌ واحدةٌ بالرقم نفسِه لا تُكتب مرّتين */
            $table->unique(['user_id', 'notif_key', 'cycle']);
            /* قراءةُ الجرس: صفوفُ هذا المستخدم المفتوحةُ في نشاطه */
            $table->index(['user_id', 'business_id', 'resolved_at']);
            /* وسجلُّ المكتملة يُقرأ بالأحدث */
            $table->index(['user_id', 'business_id', 'updated_at']);
        });
    }

    public function down(): void
    {
        Schema::dropIfExists('notification_states');
    }
};
