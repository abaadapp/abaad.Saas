/**
 * «تم» على لوحة مفاتيح الآيباد تُنهي الكتابة، ولا تحفظ ولا تقفز.
 *
 * المتصفّحات ترسل النموذج حين تُضغط Enter داخل حقلٍ فيه — «الإرسال الضمني»
 * — أو تنقل التركيزَ إلى الحقل الذي يليه. على سطح المكتب هذا متوقَّع: يد
 * المستخدم على المفاتيح ويعرف أنه ضغط Enter. وعلى الآيباد ليس كذلك:
 * المفتاح مكتوبٌ عليه «تم»، وهي كلمةٌ تعني إنهاءَ الكتابة في الخانة لا
 * حفظَ النموذج ولا الانتقالَ منه. فيكتب التاجر اسم المنتج، يضغط «تم»
 * ليغلق اللوحة ويكمل بقيّة الحقول، فيُحفظ المنتج ناقصًا — أو يجد نفسَه في
 * حقلٍ آخر واللوحةُ ما زالت تغطّي نصفَ الشاشة.
 *
 * فعلى الأجهزة التي لوحتُها على الزجاج وحدها: Enter داخل حقلٍ تُنهي
 * التركيز فقط، والحفظ يبقى بضغطة الزرّ. وسطح المكتب لا يتغيّر.
 *
 * ═══ والحقلُ خارج النموذج حقلٌ مثلُه ═══
 *
 * كان الشرط `if (!el.form) return` — فما لا نموذجَ له لا يُمسّ. وكان ذلك
 * صحيحًا لو أنّ الغرض منعُ الإرسال الضمنيّ وحده؛ لكنّ الغرض الثاني — وهو
 * ما يراه صاحبُ المحلّ — أن **تنغلق اللوحة**. وبلا نموذجٍ لا تنغلق: يضغط
 * «تم» فلا يقع شيء، وتبقى اللوحة تغطّي نصفَ الآيباد حتى ينقر في فراغ
 * الصفحة إن وجد فراغًا.
 *
 * ومسحُ المستودع أخرج **١٧٤ حقلًا خارج أيّ نموذج** في أربعةٍ وخمسين ملفًّا،
 * وأكثرُها في الشاشات التي يُكتب فيها أكثرُ الكلام: فاتورة العميل، وأمر
 * الشراء، ونافذة الدفع. فالقاعدة صارت على الحقل لا على نموذجه.
 *
 * ثلاثة استثناءات مقصودة:
 * — النماذج التي تحمل `data-enter-submits`: الدخول والرمز واستعادة كلمة
 *   المرور. حقلٌ أو حقلان و«اذهب» على المفتاح، والمنعُ فيها إزعاجٌ لا حماية.
 * — ما استهلك المفتاح قبلنا (defaultPrevented): حقل الباركود يقرأ Enter من
 *   الماسح، وحقل الكوبون يطبّقه. المستمع في الفقاعة فيصل بعد React. وهذا
 *   الاستثناء يحرس الماسح: كاشيرٌ يمسح صنفًا بعد صنف لا يحتمل أن يفقد
 *   الحقلُ تركيزَه بعد كلّ مسحة.
 * — textarea: Enter فيها سطرٌ جديد ولا تُرسل نموذجًا أصلًا.
 */

/**
 * جهازٌ لوحتُه على الزجاج — لا تحت الأصابع.
 *
 * ═══ ولمَ لا يكفي استعلامُ الوسائط وحده ═══
 *
 * `(hover: none) and (pointer: coarse)` هو السؤالُ الصحيح، ويصدق على
 * الهاتف. ولا يصدق على الآيباد: سفاري عليه يفتح المواقع بوضع «عرض كموقع
 * حاسوب» افتراضًا منذ iPadOS 13، ويُخفي لمسَه عمدًا ليُرى حاسوبًا — فيردّ
 * `hover: hover` و`pointer: fine`. فكانت الدالّةُ تخرج من أوّل سطرٍ على
 * الجهاز الذي كُتبت له، ويبقى مفتاحُ «تم» يفعل ما تفعله سفاري: يقفز إلى
 * الحقل التالي واللوحةُ مفتوحة. وهذا ما كان يراه صاحبُ المحلّ.
 *
 * وعددُ نقاط اللمس لا يُخفى: سفاري يبدّل الوكيلَ والاستعلام ويُبقي
 * `maxTouchPoints` على خمس، ولذلك يُكشف به وضعُ الحاسوب على الآيباد. ولا
 * حاسوبَ من Apple يبلّغ لمسًا — فالشرطان معًا آيبادٌ أو هاتفٌ طُلب له وضعُ
 * الحاسوب، لا سواهما.
 *
 * والشرطُ الثاني مقصورٌ على أجهزة Apple كي يبقى الوعدُ الأوّل: حاسوبٌ
 * محمولٌ بشاشةٍ لمسيّة تحته لوحةُ مفاتيحَ حقيقيّة، ومنعُ Enter فيه إزعاجٌ
 * لا حماية.
 */
function typesOnGlass(): boolean {
    if (window.matchMedia?.('(hover: none) and (pointer: coarse)').matches) {
        return true;
    }

    return (navigator.maxTouchPoints ?? 0) > 1 && /Macintosh/.test(navigator.userAgent);
}

/**
 * أزرارٌ لا يُكتب فيها — فلا لوحةَ تُغلق ولا تركيزَ يُنهى.
 *
 * وتُترك على حالها: Enter عليها تضغطها، ومنعُ ذلك يجعل زرًّا لا يعمل.
 */
const BUTTONS = ['submit', 'button', 'reset', 'image'];

export function enterEndsTypingOnTouch(): void {
    if (!typesOnGlass()) {
        return;
    }

    document.addEventListener('keydown', (event) => {
        if (event.key !== 'Enter' || event.defaultPrevented) return;

        const el = event.target;
        if (!(el instanceof HTMLInputElement)) return;
        // نموذجٌ أذِن لـEnter أن ترسله — ولا شأن لنا بمفتاحه
        if (el.form?.dataset.enterSubmits !== undefined) return;
        if (BUTTONS.includes(el.type)) return;

        event.preventDefault();
        el.blur();
    });
}
