/**
 * حالة الشركة كما تقرأها لوحة المنصة.
 *
 * اللفظان معًا لأن الكلمة تُكتب بشكلين في القاعدة — بالشدّة وبدونها —
 * ونظيرُها على الخادم `Tenancy::BLOCKED_BUSINESS`. ومقارنةٌ بلفظٍ واحد تجعل
 * شركةً معطَّلة تبدو عاملة في الشاشة وحدها، فيُعرض عليها زرُّ تعطيلٍ لا
 * يفعل شيئًا ولا يُعرض زرُّ إعادة التشغيل الذي تحتاجه.
 */
const DISABLED = ['معطل', 'معطّل'];

export const isDisabled = (status: string): boolean => DISABLED.includes(status.trim());
