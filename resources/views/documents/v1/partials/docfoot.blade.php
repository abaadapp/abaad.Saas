{{--
    تذييلُ الورقة — اسمُ المتجر، صغيرًا وهادئًا في الوسط.

    ═══ ولمَ خفّ بعد أن كان شريطًا ═══

    كان خطًّا وسطرَ هويّةٍ ورقمَ مستند. وفي المرجع المعتمد سطرٌ واحدٌ
    صغيرٌ في أسفل الصفحة لا خطَّ فوقه: اسمُ المُصدِر في الوسط، ورقمُ
    الصفحة ورقمُ المستند في الطرف.

    والرقمُ الضريبيُّ ليس هنا: صار حقلًا مسمّى في كتلة الهويّة أعلى
    الورقة — انظر `partials/identity` — فمن يراجع يجده باسمه لا في
    حاشية.

    ورقمُ الصفحة ورقمُ المستند ليسا هنا أيضًا: يرسمهما
    `MpdfDriver::pageNumbers` في تذييل **كلّ** صفحة لا في آخرها وحدها —
    وصفحةٌ تسقط من حزمةٍ في مكتب محاسبةٍ لا يُعرف إلى أين تعود.

    ولا يُطبع لمتجرٍ بلا اسم: سطرٌ خاوٍ في أسفل الورقة حبرٌ بلا خبر.
--}}
@php
    $brand = \App\Support\Paper::brand($business ?? null, $vatNumber ?? '');
    $name = trim((string) $brand['name']);
@endphp

@if ($name !== '')
    <table class="docfoot">
        <tr><td>{{ $name }}</td></tr>
    </table>
@endif
