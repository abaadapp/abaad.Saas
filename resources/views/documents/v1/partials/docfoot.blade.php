{{--
    تذييلُ الهويّة — آخرُ سطرٍ على الورقة.

    ═══ ولمَ يتكرّر ما في الترويسة ═══

    لا يتكرّر: الترويسةُ تحمل الاسمَ والشعار، وهذا يحمل **ما يُثبت** —
    السجلَّ التجاريَّ والرقمَ الضريبيَّ والهاتف. وهي بياناتٌ تُطلب في
    المراجعة والتدقيق ولا تُقرأ في السطر الأوّل، فمكانُها أسفلُ الورقة كما
    في كلّ ورقٍ رسميّ.

    وكانت هذه السطورُ مبعثرةً: بعضُها في بطاقة البائع وبعضُها لا يُطبع
    أصلًا، فتخرج فاتورةٌ بلا رقمٍ ضريبيٍّ في موضعٍ يجده المحاسب.

    ═══ ورقمُ المستند معها ═══

    صفحةٌ تسقط من حزمةٍ في مكتب محاسبةٍ لا يُعرف إلى أين تعود. ورقمُ
    الصفحة «٢ / ٣» يأتي من تذييل المحرّك على **كلّ** صفحة — انظر
    `MpdfDriver::frame` — وهذا يُثبّت الهويّةَ على آخرها.

    ولا يُطبع فارغًا: متجرٌ لم يكتب عنوانًا ولا هاتفًا ولا رقمًا ضريبيًّا
    لا يخرج له شريطٌ فيه اسمُه وحده فوق خطٍّ بلا سبب.
--}}
@php
    $brand = \App\Support\Paper::brand($business ?? null, $vatNumber ?? '');

    /*
     * والاسمُ ليس هنا — الترويسةُ قالته مرّةً.
     *
     * ═══ وهذا اعتراضُ اختبارٍ قائم، وكان محقًّا ═══
     *
     * بُني هذا الشريطُ أوّلًا باسم المتجر ورقمه الضريبيّ معًا، فأسقط
     * `test_the_paper_names_both_sides`: الاسمُ يُقرأ مرّتين على ورقةٍ
     * واحدة. والاسمُ في الترويسة بمقاسٍ يُرى من بعيد، وتكرارُه في الحاشية
     * بمقاسٍ صغير لا يضيف خبرًا — يضيف حبرًا.
     *
     * وما يضيف خبرًا هو ما لا تحمله الترويسة: الرقمُ الضريبيّ. وهو الذي
     * يُطلب في التدقيق، ولا يُقرأ في السطر الأوّل.
     */
    $identity = implode('  ·  ', array_filter($brand['legal']));

    $ref = trim((string) ($doc['number'] ?? ''));
    $rtl = \App\Support\Paper::rtl();
@endphp

@if (count($brand['legal']) > 0)
    <table class="docfoot">
        <tr>
            <td style="width:70%; text-align:{{ $rtl ? 'right' : 'left' }}">{{ $identity }}</td>
            @if ($ref !== '')
                <td style="width:30%; text-align:{{ $rtl ? 'left' : 'right' }}">
                    <span class="ltr">{{ $ref }}</span>
                </td>
            @endif
        </tr>
    </table>
@endif
