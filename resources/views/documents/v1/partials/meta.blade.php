{{--
    حقولُ المستند — رقمُه وتواريخُه ومراجعُه، في صدر الورقة.

    ═══ وكانت شريطًا بأرضيّة ═══

    أربعةُ حقولٍ في شريطٍ رماديٍّ بعرض الورقة تحت الأطراف. وفي المرجع
    المعتمد عمودٌ من حقولٍ في الترويسة نفسِها، بلا أرضيّةٍ ولا خطوط: كلُّ
    حقلٍ ثلاثةُ أسطرٍ — تسميةٌ فوق تسميةٍ فوق قيمة — والفراغُ بينها هو ما
    يقسمها.

    وهو أوضح: من يبحث عن تاريخ الاستحقاق يجده حيث يجد رقمَ الفاتورة، في
    عمودٍ واحدٍ يُمسح بنظرة، لا في شريطٍ يُقرأ عرضًا.

    ═══ وعمودٌ واحدٌ لا يتّسع لكلّ ورقة ═══

    للمرجع أربعةُ حقول. ولفاتورةِ بيعٍ آجلةٍ في هذا النظام سبعة: رقمٌ
    وتاريخٌ ووسيلةُ دفعٍ وشروطُه وتاريخُ استحقاقٍ وفرعٌ وموظّف. وسبعةٌ في
    عمودٍ واحدٍ بُرجٌ يبلغ ثلثَ الورقة، ويترك ثلثَي عرضِ الترويسة بياضًا
    إلى جانبه — ثمّ يدفع الملاحظاتِ والتذييلَ إلى صفحةٍ ثانية.

    فما زاد على أربعةٍ يُقسَم عمودين. والقسمةُ بالنصف لأعلى: أربعةٌ ثمّ
    ثلاثة — لا ثلاثةٌ ثمّ أربعة — فيبقى العمودُ الأوّل هو الأطول كما
    تتوقّعه العين.

    ولا يُعرض حقلٌ لا معنى له على هذه الورقة: «تاريخ الاستحقاق» على سند
    تسليمٍ حقلٌ نُسي أنّه لا يخصّه. فالقائمةُ تُبنى في كلّ قالبٍ بما
    يخصّه — انظر `$metaCells` في كلٍّ منها.

    المتغيّر: `$cells` قائمةُ `['label' => مفتاحٌ عربيّ, 'value' => ...]`.
--}}
@php
    $cells = array_values($cells ?? []);
    /* وحدُّ القسمة أربعة: خمسةٌ تُقرأ عمودين أريحَ ممّا تُقرأ برجًا */
    $split = count($cells) > 4;
    $half = (int) ceil(count($cells) / 2);
@endphp

@if ($split)
    {{-- وجدولُ القسمة مقيَّدُ التخطيط: انظر §١٠ في `DESIGN_SYSTEM` --}}
    <table class="metasplit">
        <tr>
            <td>@include('documents.v1.partials.fields', ['fields' => array_slice($cells, 0, $half)])</td>
            <td class="gap"></td>
            <td>@include('documents.v1.partials.fields', ['fields' => array_slice($cells, $half)])</td>
        </tr>
    </table>
@else
    @include('documents.v1.partials.fields', ['fields' => $cells])
@endif
