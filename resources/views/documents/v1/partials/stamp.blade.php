{{--
    خَتمُ الحالة — كلمةٌ في إطار، لا نقطةٌ ملوّنة.

    ═══ ولمَ لا يحملها اللون ═══

    الورقةُ تُطبع أبيضَ وأسودَ في أكثر المكاتب، وتُصوَّر بعد ذلك مرّةً أو
    مرّتين. فحالةٌ يقولها اللونُ وحده — أخضرُ للمعتمَد وأحمرُ للملغى —
    تُمحى عند أوّل طابعةِ ليزر، فتخرج نقطةٌ رماديّةٌ لا تقول شيئًا.

    فالخَتمُ إطارٌ وكلمةٌ ومقاسٌ صغيرٌ متباعدُ الحروف: يُقرأ على الشاشة
    وعلى الورق الرماديّ سواءً. وهو شرطٌ صريحٌ في المواصفة: «لا تجعل اللون
    عنصرًا ضروريًّا لفهم الحالة».

    ═══ ولا يُطبع إلّا ما في النموذج ═══

    `$doc['status']` تأتي من حقل الحالة في الجدول نفسِه — `$po->status`
    و`$note->status` — لا من اشتقاقٍ في القالب. ومستندٌ لا حالةَ له (سندُ
    تسليمٍ مثلًا) لا يُختم بشيء: خانةٌ خاويةٌ تحت الرقم تُقرأ حقلًا نُسي.
--}}
@php
    $stamp = trim((string) ($doc['status'] ?? ''));
@endphp

@if ($stamp !== '')
    {{-- وشريطٌ بعرض اللوحة لا رقعةٌ منكمشة: انظر سببَ ذلك في `layout` --}}
    <div class="stamp">{{ $stamp }}</div>
@endif
