{{--
    رموزُ التصميم — الطبقةُ التي تجعل أوراقَ النظام ورقةً واحدة.

    ═══ ولمَ تُكتب القيمةُ مرّتين ═══

    الورقةُ تُقرأ في محرّكين: mpdf ليخرج الـPDF، والمتصفّح لتُرى في المعاينة
    وتُرسَل رابطًا. والمتصفّحُ يفهم `var(--document-primary)`، وmpdf **لا
    يفهمها** — يتجاهل الإعلان صامتًا فتخرج الورقة بلا لون، ولا خطأ يقول لماذا.

    فكلُّ قاعدةٍ تُكتب مرّتين: `var()` أوّلًا للمتصفّح، ثمّ القيمةُ الحرفيّة
    بعدها. والأخيرةُ تغلب في المحرّكين معًا — فالشكلُ واحدٌ فيهما لا شكلان،
    وهو شرطُ «HTML هو المصدر البصريّ والـPDF نسخةٌ منه».

    ═══ والقياسُ بالنقطة لا بالبكسل ═══

    الورقةُ مقاسُها بالمليمتر، والبكسل في mpdf يُحوَّل بمعامل شاشةٍ لا معنى
    له على ورق. و`$s()` تضرب كلَّ مقاسٍ بمعامل الخطّ الذي اختاره التاجر —
    فتكبر الورقةُ معًا، لا سطرُ الجسد وحده فيصير الجدولُ أصغر ممّا حوله.

    ═══ وما تغيّر في هذه النسخة، ولماذا ═══

    كانت الورقةُ **صناديق**: لوحةٌ ملوّنةٌ للعنوان، وبطاقتان بأرضيّةٍ
    وحافّةٍ للطرفين، وشريطُ تعريفٍ مشبَّك، ورأسُ جدولٍ مصمتٌ بعرض الورقة،
    وكتلةٌ داكنةٌ للإجمالي بنصٍّ معكوس، وشريطٌ رماديٌّ للملاحظات. ستّةُ
    أسطحٍ ملوّنةٍ على ورقةٍ واحدة — تُقرأ لوحةَ تحكّمٍ في متصفّح، لا ورقةً
    تخرج من طابعة وتُحفظ في ملفّ.

    وصارت **بياضًا وخطوطًا**: لا أرضيّةَ إلّا اثنتان فاتحتان جدًّا — شريطُ
    التعريف ولوحةُ المجاميع — وما عداهما فراغٌ وشعراتٌ فاصلة. ولونُ التاجر
    ينفَق في أربعة مواضعَ لا أكثر: مسطرةٌ تفتتح الورقة، وعنوانُ المستند،
    وخطٌّ تحت رأس الجدول، وخطُّ الإجمالي مع رقمه. وهو معنى «branding color
    كـaccent فقط، وليس كخلفيات ثقيلة».

    والتسلسلُ البصريُّ صار بالمقاس: عنوانُ المستند ثمانٍ وعشرون نقطة،
    والإجماليُّ خمسَ عشرة، والجسدُ عشر، والدلائلُ سبعٌ ونصف. فيُعرف أين
    يُنظر بلا أن يُصبغ شيء.

    والألوانُ كلُّها مشتقّةٌ من لونٍ واحدٍ يختاره التاجر، ومحسوبةُ التباين
    في `Theme` — فورقةُ من لم يختر شيئًا تخرج بحبرٍ داكنٍ موقّر، لا باهتة.
--}}
@php
    use App\Support\Document\PaperSize;
    use App\Support\Document\Theme;

    $t = $tokens;

    /* مقاسُ الورقة — من السجلّ نفسِه الذي يُبنى به المحرّك */
    $size = PaperSize::of($paper ?? PaperSize::A4);
    $scale = (float) ($t['scale'] ?? 1.0);

    /** مقاسٌ بالنقطة، مضروبًا بمعامل التاجر */
    $s = fn (float $size) => round($size * $scale, 2) . 'pt';

    /** مقاسٌ لا يتبع الخطّ — الهوامشُ والحوافّ لا تكبر بكبر النصّ */
    $fx = fn (float $size) => round($size, 2) . 'pt';

    /*
        واتّجاهُ الورقة من مصدرٍ واحد.

        وmpdf لا يعرف `margin-inline-start` ولا `text-align: start`، فتُحسب
        الجهةُ هنا مرّةً وتُكتب فيزيائيّةً. وهو ما يجعل القالبَ واحدًا
        للغتين بلا نسخةٍ ثانية — الحسابُ في PHP لا الشرطُ في كلّ قاعدة.
    */
    $rtl = \App\Support\Paper::rtl();
    $start = $rtl ? 'right' : 'left';
    $end = $rtl ? 'left' : 'right';
@endphp
<style>
    :root {
{!! Theme::css($t) !!}
    }

{!! PaperSize::css($size) !!}

    /*
        و`border-box` على كلّ شيء — لا على الصندوق وحده.

        بلا ذلك يُضاف الحشوُ إلى العرض المصرَّح به: عمودٌ عرضُه ٢٠٪ وحشوُه
        ٧pt يخرج أعرضَ من خُمس الورقة، فتتجاوز الأعمدةُ مجتمعةً حدَّها
        ويُقلّص المحرّكُ الجدولَ ليُلائمه — وهو تصغيرٌ لا يطلبه أحد.
    */
@include('documents.v1.partials.webfont')
    {{--
        وخطُّ الورقة واحدٌ في المحرّكين — وطريقُه إلى كلٍّ منهما غيرُ الآخر.

        هذا السطرُ للمتصفّح: `'IBM Plex Sans Arabic'` هو اسمُ الأسرة في
        `@font-face` أعلاه. وmpdf **لا يقرأ محدّدَ `*` أصلًا** — قِسناه:
        بُدّل الاسمُ هنا إلى خطٍّ آخر فخرج الـPDF على حاله. خطُّه يصله من
        `default_font` في `MpdfDriver::base`.

        و`ibmplexsansarabic` بلا فراغ أوّلًا احتياطًا: اسمُ الأسرة في سجلّ
        mpdf لا يحتمل فراغًا، فلو قرأ المحدّدَ يومًا وجد ما يعرفه. والمتصفّحُ
        يتخطّاه — ليس خطًّا عنده — إلى الاسم بعده.
    --}}
    * { font-family: ibmplexsansarabic, 'IBM Plex Sans Arabic', sans-serif; box-sizing: border-box; }

    body {
        /* ولا هامشَ افتراضيًّا من المتصفّح: ٨ بكسل تُزيح الورقة عن حدّها */
        margin: 0;
        padding: 0;
        direction: {{ $rtl ? 'rtl' : 'ltr' }};
        text-align: {{ $start }};
        color: var(--document-text); color: {{ $t['text'] }};
        background: var(--document-background); background: {{ $t['background'] }};
        font-size: {{ $s(Theme::GEOMETRY['text_base']) }};
        line-height: 1.55;
    }

    /* ————————————————— النصّ ————————————————— */

    .muted { color: var(--document-muted); color: {{ $t['muted'] }}; }
    .faint { color: var(--document-faint); color: {{ $t['faint'] }}; }
    .brandink { color: var(--document-primary-ink); color: {{ $t['primary_ink'] }}; }
    .xs { font-size: {{ $s(Theme::GEOMETRY['text_xs']) }}; }
    .sm { font-size: {{ $s(Theme::GEOMETRY['text_sm']) }}; }
    .md { font-size: {{ $s(Theme::GEOMETRY['text_md']) }}; }
    .lg { font-size: {{ $s(Theme::GEOMETRY['text_lg']) }}; }
    .b  { font-weight: bold; }
    .c  { text-align: center; }

    /*
        العنوانُ الصغير فوق كلّ كتلة — حروفٌ متباعدة ومقاسٌ صغير.

        وهو ما يحمل التسلسلَ البصريّ بدل الخطوط الثقيلة: القارئ يعرف أين
        هو من الورقة بالمقاس والفراغ، لا بإطارٍ حول كلّ شيء.
    */
    /*
        ولونُه باهتٌ لا لونُ التاجر.

        عنوانُ كتلةٍ صغيرٌ مكرّرٌ ستَّ مرّاتٍ على الورقة — «البائع»،
        «العميل»، «ملاحظات» — بلون التاجر يجعل اللونَ أثاثًا. والعنوانُ
        دليلٌ لا خبر: يُقرأ حين يُبحث عنه، ويبقى تحت ما يدلّ عليه.
    */
    .eyebrow {
        font-size: {{ $s(Theme::GEOMETRY['text_xs']) }};
        font-weight: bold;
        color: var(--document-faint); color: {{ $t['faint'] }};
        letter-spacing: 0.1em;
        {{--
            وسطرُه متّسعٌ لأنّ هامشَه لا يصل.

            mpdf يُسقط هوامشَ الكتل داخل الخلايا — انظر `table.asidestack` —
            وعنوانُ الكتلة يقع داخل خليّةٍ في كلّ موضعٍ يُستعمل فيه: بطاقةُ
            الطرف، وعمودُ الملاحظات. فيُحجَز فراغُه في سطره هو.
        --}}
        line-height: 2;
    }

    /*
        المبالغُ إلى الطرف المقابل دائمًا، ولا تنكسر.

        مبلغٌ ومعه رمزُ العملة كان يُقسَم سطرين في عمودٍ ضيّق فيُقرأ رقمين.
        و`direction: ltr` لأنّ الرقم يُقرأ من اليسار في اللغتين معًا.
    */
    .amt { text-align: {{ $end }}; white-space: nowrap; direction: ltr; }
    .num { text-align: center; direction: ltr; }

    /*
        وكلُّ ما يُقرأ من اليسار داخل سطرٍ عربيّ يُعزَل.

        رقمُ فاتورةٍ أو آيبانٌ أو بريدٌ داخل نصٍّ عربيّ يتبع اتّجاهَ السطر لا
        اتّجاهَ نفسِه، فينقلب طرفاه. و`unicode-bidi: isolate` تفصله عمّا حوله
        فيُقرأ كما كُتب — انظر ReceiptTemplate::printableHtml للسطور الحرّة.
    */
    .ltr { direction: ltr; unicode-bidi: isolate; display: inline-block; }

    {{--
        ونصُّ البشر: اتّجاهُه من لغته، ومحاذاتُه من الورقة.

        و`dir` يحملهما معًا — فسمةُ `rtl` على اسم صنفٍ عربيٍّ داخل ورقةٍ
        إنجليزيّة تصحّح ترتيبَ حروفه **وتزيحه إلى يمين خليّته**، فيخرج
        عمودُ الأصناف مبعثرًا: عربيُّه يمينًا وإنجليزيُّه يسارًا في جدولٍ
        واحد.

        فتُفصل المحاذاة: تتبع الورقةَ دائمًا، ويبقى لـ`dir` ترتيبُ الحروف
        وحدَه — وهو ما أردناه منه.
    --}}
    .bidi { text-align: {{ $start }}; }

    /* ————————————————— الغلاف والترويسة ————————————————— */

    .cover {
        width: 100%;
        height: {{ $fx(Theme::GEOMETRY['cover_height']) }};
        border-radius: var(--document-radius-lg); border-radius: {{ $fx($t['radius_lg']) }};
        overflow: hidden;
        margin-bottom: {{ $fx(14) }};
    }

    /*
        وبلا صورةٍ لا تخرج الورقة فارغة.

        شريطٌ بلون التاجر أنحفُ من الصورة بكثير: يقول «هذه ورقةُ فلان» ولا
        يأكل ثلثَ الصفحة لأجل لونٍ واحد. ومتجرٌ لم يرفع غلافًا لا يجب أن
        تكون ورقتُه أسوأ — يجب أن تكون **أبسط**.
    */
    .cover-plain {
        width: 100%;
        {{--
            وأربعُ نقاطٍ لا عشر.

            عشرُ نقاطٍ بلونٍ مصمتٍ بعرض الورقة كتلةُ لونٍ لا لمسة: تفتتح
            الورقةَ بأثقل ما فيها قبل أن تُقرأ كلمة. والمرجعُ المعتمد يجعل
            لونَ التاجر **لمسةً** — خطًّا وحبرًا وحدًّا — لا أرضيّات.
        --}}
        height: {{ $fx(4) }};
        border-radius: var(--document-radius); border-radius: {{ $fx($t['radius']) }};
        background: var(--document-primary); background: {{ $t['primary'] }};
        margin-bottom: {{ $fx(14) }};
    }

    /*
        ومسطرةُ الترويسة — الخطُّ الذي تفتتح به الورقة.

        ═══ ولمَ تُطبع لمن لم يختر لونًا ═══

        الغلافُ زينةٌ: شريطٌ عريضٌ بلونٍ اختاره التاجر، ومن لم يختر لا
        يُطبع له. وهذه غيرُه — بنيةٌ لا زينة: هي ما يفصل رأسَ الورقة عن
        حافّة الصفحة، كما يفعل خطُّ الترويسة في كلّ ورقٍ رسميّ. وارتفاعُها
        ٢٫٥ نقطة: حبرٌ لا يُذكر مقابل ورقةٍ لها بداية.

        ومن لم يختر لونًا فلونُه الحبرُ الداكن الافتراضيّ — فتخرج مسطرةً
        موقّرة لا باهتة.
    */
    .brandrule {
        width: 100%;
        height: {{ $fx(2) }};
        background: var(--document-primary); background: {{ $t['primary'] }};
        margin-bottom: {{ $fx(13) }};
        font-size: 0;
        line-height: 0;
    }

    .head { width: 100%; table-layout: fixed; }
    .head td { vertical-align: top; border: none; padding: 0; }

    /*
        ورأسُ الورقة يُغلق بخطٍّ بلون التاجر لا بفراغ.

        الفراغُ وحده يترك الترويسةَ عائمةً فوق الأطراف بلا حدٍّ يفصلهما،
        فتُقرأ الورقةُ كتلةً واحدةً من أعلاها إلى جدولها. والخطُّ الرفيع
        يقول «انتهت الهويّة، بدأ المستند».
    */
    {{--
        وصار شعرةً محايدة بعد أن كان حدًّا بلون التاجر.

        الورقةُ المعتمدة تُنفق لونَ التاجر في ثلاثة مواضعَ لا أكثر: مسطرةٌ
        تفتتحها، وعنوانُ المستند، وخطُّ الإجمالي. وخطٌّ رابعٌ ملوّنٌ تحت
        الترويسة يجعل اللونَ أثاثًا — فيفقد ما يشير إليه.
    --}}
    .headrule {
        border-bottom: {{ $fx(0.5) }} solid {{ $t['border'] }};
        margin: {{ $fx(14) }} 0 {{ $fx(Theme::GEOMETRY['section_gap']) }} 0;
        font-size: 0;
        line-height: 0;
    }

    {{--
        عنوانُ المستند — أكبرُ ما على الورقة، ولا صندوقَ حوله.

        ═══ ولمَ كبر ═══

        كان عشرين نقطةً في لوحةٍ ملوّنة: مقاسٌ يوازي اسمَ المتجر في العمود
        المقابل، فتبدأ الورقةُ بكتلتين تتنازعان العين. والعنوانُ هو ما
        يُقرأ أوّلًا — «فاتورة ضريبية»، «أمر شراء» — فيُفرد له مقاسٌ لا
        ينازعه شيء، ويُترك حوله بياض.
    --}}
    .doctype {
        font-size: {{ $s(Theme::GEOMETRY['text_display']) }};
        font-weight: bold;
        color: var(--document-primary-ink); color: {{ $t['primary_ink'] }};
        {{--
            وسطرُه متّسعٌ — والاتّساعُ هنا ليس ذوقًا.

            قِيس في الـPDF: mpdf يرسم حروفَ السطر داخل صندوقٍ ارتفاعُه
            `line-height`، **ولا يوسّع الصندوقَ لما ينزل تحته**. ونزلاتُ
            العربيّة عند ثمانٍ وعشرين نقطةً — ياءُ «ضريبية» ونونُ «إذن» —
            تتجاوز ١٫١ من المقاس، فتنزل على «رقم الفاتورة» تحتها وتركبه.

            وحشوٌ أو هامشٌ لا يُصلحها: المحرّك يزيح الكتلةَ التاليةَ ولا
            يزيح الحروفَ النازلة، فيتباعد السطران ويبقى التداخل. والمتصفّحُ
            يوسّع الصندوقَ للنزلات فلا يظهر العطبُ في المعاينة أصلًا.
        --}}
        line-height: 1.15;
        letter-spacing: -0.02em;
    }

    /*
        ورقمُ المستند في رقعةٍ مصمتة — لا سطرًا رماديًّا تحت العنوان.

        هو ما يُذكر في المكالمة ويُبحث عنه في البريد ويُكتب على الحوالة.
        ورقعةٌ بلون التاجر تجعله أوّلَ ما تقع عليه العين بعد نوع المستند،
        وتفصله عن التواريخ التي حوله فلا يُخلط برقمٍ آخر على الورقة.
    */
    {{--
        لوحةُ المستند — نوعُه ورقمُه وحالتُه في كتلةٍ واحدة.

        ═══ ولمَ لا تنكمش إلى محتواها ═══

        جُرّب الرقمُ رقعةً مصمتةً تنكمش، مرّةً بـ`display: inline-block`
        ومرّةً بجدولٍ داخليّ. فالأولى خرجت متراكبةً على عنوانها: mpdf يرسم
        الصندوقَ بحشوه ولا يوسّع صندوقَ السطر له. والثانيةُ سحقت أعمدةَ
        الترويسة كلَّها: جدولٌ بعرض ١٠٠٪ داخل خليّةٍ بعرض ٥٠٪ يُربك حسابَ
        العرض الأدنى عنده.

        وكلا العطبين لا يظهر في المتصفّح — يظهر في الـPDF وحده. وهو ما
        تعنيه المواصفةُ بـ«لا تفترض أن نجاح HTML يعني نجاح PDF».

        فاللوحةُ كتلةٌ بعرض عمودها: لا انكماشَ ولا تداخل، والرقمُ والخَتمُ
        يملآنها فيُقرآن شريطين تحت العنوان.
    --}}
    {{--
        والعرضُ مُصرَّحٌ به في كلّ كتلةٍ داخل خليّة.

        قِيس في الـPDF: mpdf يمدّ كتلةَ `div` عبر الورقة حين تقع في مجرى
        الجسد، ويُقلّصها إلى محتواها حين تقع **داخل خليّة جدول**. فبطاقةُ
        الطرف تخرج ملتصقةً باسم المورّد لا تملأ نصفَ الورقة، ولوحةُ المستند
        تخرج بعرض كلمتين. والمتصفّحُ يمدّ الاثنتين — فالعطبُ في الـPDF وحده.
    --}}
    {{--
        ولا أرضيّةَ لها بعد اليوم — ولا حدَّ ولا حشو.

        ═══ وما بقي من القاعدة القديمة، ولمَ بقي ═══

        الأرضيّةُ ذهبت: «Header نظيف بدون صناديق» شرطٌ صريح، ولوحةٌ ملوّنةٌ
        في ركن الترويسة أوّلُ ما يُقرأ صندوقًا. وبقي **المحدِّدُ نفسُه**
        — `.head td.idpanel` لا `.idpanel` — لأنّ الدرسَ الذي وُلد منه لم
        يذهب: mpdf يُقلّص كتلةَ `div` إلى عرض نصّها داخل خليّة جدول، فأيُّ
        أرضيّةٍ أو حدٍّ يعود إلى هذا العمود يومًا يجب أن يقع على الخليّة.
        وهو المكتوب في `design-system/DESIGN_SYSTEM.md` §10.

        والفصلُ الآن بالمقاس والبياض: عنوانٌ بثمانٍ وعشرين نقطةً في طرف،
        وهويّةٌ في مقابله — لا لونَ بينهما.
    --}}
    .head td.idpanel {
        padding: {{ $fx(2) }} 0 0 0;
    }
    {{--
        وعمودُ المستند صفوفٌ: انظر سببَ ذلك في `layout`.

        ═══ ومحاذاتُه مكتوبةٌ هنا لا موروثة ═══

        الورقةُ تُرسَل قصاصةَ HTML بلا `<!DOCTYPE>` — تُحقن في معاينةٍ وفي
        صفحةٍ عامّة — فيقرؤها المتصفّحُ في **وضع التساهل**، وفيه لا يرث
        الجدولُ `text-align` ممّا حوله (قاعدةٌ من عهد الجداول القديمة).
        فتخرج الترويسةُ محاذيةً للطرف المقابل في المتصفّح ولطرفها الصحيح
        في الـPDF — والشكلان يفترقان بلا أن يُخطئ أحد.

        فتُكتب صراحةً. والحسابُ في PHP مرّةً كعادة هذه الورقة.
    --}}
    table.idstack { width: 100%; border-collapse: collapse; }
    table.idstack td { border: none; padding: 0 0 {{ $fx(9) }} 0; text-align: {{ $end }}; }
    table.idstack tr:last-child td { padding-bottom: 0; }
    /* وخليّةٌ فاصلةٌ بلا محتوى: الفراغُ بين الكتلتين بلا `border-spacing` */
    .head td.gap { padding: 0; }

    .docnum {
        font-size: {{ $s(Theme::GEOMETRY['text_md']) }};
        font-weight: bold;
        {{-- ورقمُه بحبر الورقة لا بلون التاجر: اللونُ للعنوان وحده في هذا الركن --}}
        color: var(--document-text); color: {{ $t['text'] }};
        direction: ltr;
        line-height: 1.4;
        margin-bottom: {{ $fx(4) }};
    }
    .docnum-cap {
        font-size: {{ $s(Theme::GEOMETRY['text_xs']) }};
        color: var(--document-faint); color: {{ $t['faint'] }};
        letter-spacing: 0.06em;
    }

    /*
        والحالةُ خَتمٌ لا لون.

        الورقةُ تُطبع أبيضَ وأسودَ في أكثر المكاتب، فحالةٌ يحملها اللونُ
        وحده تُمحى عند أوّل طابعة. فالخَتمُ إطارٌ وكلمةٌ ومقاسٌ صغير: يُقرأ
        على الشاشة وعلى الورق الرماديّ سواء. انظر `partials/stamp`.
    */
    /*
        والخَتمُ وحدَه يُترك منكمشًا — والانكماشُ هنا هو المطلوب.

        رقعةٌ بحجم كلمتها تُقرأ خَتمًا، وشريطٌ بعرض اللوحة يُقرأ عنوانَ
        قسم. فما كان عطبًا في البطاقة صوابٌ فيه.
    */
    .stamp {
        margin-top: {{ $fx(7) }};
        padding: {{ $fx(2.5) }} {{ $fx(6) }};
        border: {{ $fx(0.6) }} solid {{ $t['primary_ink'] }};
        border-radius: {{ $fx(2) }};
        background: var(--document-background); background: {{ $t['background'] }};
        font-size: {{ $s(Theme::GEOMETRY['text_xs']) }};
        font-weight: bold;
        letter-spacing: 0.08em;
        color: var(--document-primary-ink); color: {{ $t['primary_ink'] }};
    }

    .shop {
        font-size: {{ $s(Theme::GEOMETRY['text_lg']) }};
        font-weight: bold;
        line-height: 1.25;
    }

    /* ————————————————— بطاقاتُ الأطراف ————————————————— */

    /*
        الأطرافُ في جدولٍ بلا حدود — لا في `flex`.

        mpdf لا يعرف `flexbox`، وجدولٌ بخليّتين يفعل ما يُراد هنا تمامًا:
        عمودان متساويان يعلوان معًا. والفصلُ بينهما فراغٌ لا خطّ.
    */
    table.parties { width: 100%; border-collapse: separate; border-spacing: 0; margin-bottom: {{ $fx(Theme::GEOMETRY['section_gap']) }}; }
    table.parties td { vertical-align: top; border: none; padding: 0; }
    {{-- والفاصلُ خطٌّ لا فراغٌ: انظر `td.divide` أسفله --}}
    table.parties td.gap { width: 0; padding: 0; font-size: 0; line-height: 0; }

    /*
        وكلُّ طرفٍ في بطاقةٍ لها أرضيّةٌ وحافّةٌ من جهة البداية.

        ═══ ولمَ صارت بطاقةً بعد أن كانت سطورًا ═══

        سطورٌ رماديّةٌ متجاورةٌ بلا أرضيّة تُقرأ فقرةً واحدةً طويلة: عينُ
        القارئ لا تعرف أين ينتهي المورّد ويبدأ البائع إلّا بعدّ الأسطر.
        والأرضيّةُ الفاتحةُ ترسم الحدَّ بلا إطارٍ حول كلّ شيء — فتبقى
        الورقةُ مستندًا يُقرأ لا نموذجًا يُملأ.

        والحافّةُ من جهة البداية وحدها: خطٌّ واحدٌ بلون التاجر يربط
        البطاقتين بالمسطرة التي تعلو الورقة.
    */
    {{--
        ═══ وذهبت البطاقةُ وبقي ما كانت تفعله ═══

        كانت أرضيّةً بلون التاجر وحافّةً سميكةً وحوافَّ مُدوَّرة: كتلتان
        ملوّنتان تحت الترويسة مباشرة، تُقرآن «بطاقتَي واجهةٍ» لا طرفَي
        عقد. والمرجعُ المعتمد صريح: «Parties بدون cards؛ عمودان مع divider
        خفيف».

        والعملُ الذي كانت تؤدّيه — أن يعرف القارئ أين انتهى طرفٌ وبدأ
        الآخر — يؤدّيه خطٌّ رأسيٌّ شعرةٌ وبياضٌ من جانبيه. وهو أدقّ: يفصل
        العمودين مهما اختلف طولُهما، ولا يرسم حولهما صندوقين يعلوان معًا
        بارتفاع أطولهما فيخرج الأقصرُ نصفَه فارغًا.
    --}}
    table.parties td.party {
        padding: 0;
        padding-{{ $end }}: {{ $fx(16) }};
    }
    {{--
        والخطُّ على الخليّة لا على كتلةٍ داخلها — كالقاعدة نفسِها حين كانت
        أرضيّة: mpdf يُقلّص كتلَ الـ`div` داخل الخلايا، فخطٌّ على `div`
        يخرج بطول سطرٍ واحد لا بطول العمود.
    --}}
    table.parties td.divide {
        border-{{ $start }}: {{ $fx(0.5) }} solid {{ $t['border'] }};
        padding-{{ $start }}: {{ $fx(16) }};
    }
    .party-name {
        font-size: {{ $s(Theme::GEOMETRY['text_md']) }};
        font-weight: bold;
        line-height: 1.35;
    }

    {{--
        شريطُ التعريف — أعمدةٌ متجاورة، عنوانٌ صغيرٌ فوق قيمته.

        وكان جدولًا بعمودين وعنوانُه بعرض ٣٤٪، فيبقى نصفُ كلّ سطرٍ خاليًا:
        منطقةٌ ميّتةٌ بعرض راحةِ اليد بين التاريخ وجدول الأصناف.

        و`table-layout: fixed` لأنّ العرض معلَنٌ في الخليّة: بدونه يوسّع
        المحرّكُ العمودَ لأطول قيمةٍ فيه، فيعيد الشريطُ ترتيبَ نفسه كلّما
        طال اسمُ موظّف.

        وصار له أرضيّةٌ وحدٌّ بين خلاياه: أربعةُ تواريخَ متجاورةٍ في فراغٍ
        أبيض تُقرأ سطرًا واحدًا مقطوعًا، والخطُّ الرفيع بينها يقول إنّها
        أربعةُ حقول.
    --}}
    {{--
        وأرضيّتُه فاتحةٌ جدًّا — ولا خطوطَ بين خلاياه.

        شرطُ المرجع: «Metadata panel بخلفية فاتحة جدًا فقط». والخطوطُ
        الرأسيّةُ التي كانت تفصل الحقولَ صارت شبكةً صغيرةً في أعلى الورقة
        تُقرأ جدولًا ثانيًا. وما يفصل الحقولَ الآن هو ما كان يفصلها في
        المرجع: عنوانٌ صغيرٌ باهتٌ فوق قيمةٍ داكنة، وفراغٌ بينها.
    --}}
    table.metastrip {
        width: 100%;
        table-layout: fixed;
        border-collapse: collapse;
        background: var(--document-surface); background: {{ $t['surface'] }};
        border-radius: {{ $fx($t['radius']) }};
        margin-bottom: {{ $fx(Theme::GEOMETRY['section_gap']) }};
    }
    table.metastrip td {
        border: none;
        padding: {{ $fx(9) }} {{ $fx(12) }};
        vertical-align: top;
    }
    table.metastrip tr + tr td { padding-top: 0; }
    .metalabel {
        font-size: {{ $s(Theme::GEOMETRY['text_xs']) }};
        color: var(--document-faint); color: {{ $t['faint'] }};
        letter-spacing: 0.05em;
        margin-bottom: {{ $fx(1.5) }};
    }

    /* ————————————————— جدول الأصناف ————————————————— */

    /*
        رأسٌ بخطٍّ لا بأرضيّة، وصفوفٌ يفصلها شعرةٌ واحدة.

        ═══ ولمَ ذهبت الأرضيّةُ المصمتة ═══

        شريطٌ مصمتٌ بعرض الورقة بلون التاجر هو أثقلُ ما كان فيها: يُقرأ
        رأسَ جدولِ بيانات، ويجعل اللونَ مساحةً لا لمسة. والمرجعُ المعتمد
        صريحٌ في الأمرين: «Items table بخطوط minimal وعدم تحويله إلى
        spreadsheet»، و«branding color كـaccent فقط وليس كخلفيات ثقيلة».

        وما كان الشريطُ يفعله — أن يفصل الرأسَ عن الصفوف — يفعله خطٌّ
        واحدٌ أسفله بحبر التاجر: أثقلُ خطٍّ في الجدول، فيُقرأ الرأسُ رأسًا
        بلا أن يُصبغ. والعناوينُ نفسُها باهتةٌ صغيرةٌ متباعدةُ الحروف، لأنّ
        ما يُقرأ في الجدول هو البضاعةُ لا أسماءُ أعمدتها.

        ولا تناوبَ أرضيّةٍ بين الصفوف: الزِّبرةُ حيلةُ جدولِ أرقامٍ بعشرين
        عمودًا، وفاتورةٌ بأربعة أعمدةٍ تُقرأ سطرًا سطرًا على شعرةٍ فاصلة.

        ولا حدودَ رأسيّةً بين الأعمدة: القراءةُ تمشي أفقيًّا فلا تحتاج
        جدرانًا.
    */
    table.items { width: 100%; border-collapse: collapse; margin-bottom: {{ $fx(12) }}; }
    table.items th.num { text-align: center; }
    table.items th.amt { text-align: {{ $end }}; }
    table.items th {
        text-align: {{ $start }};
        padding: {{ $fx(0) }} {{ $fx(8) }} {{ $fx(6) }};
        font-size: {{ $s(Theme::GEOMETRY['text_xs']) }};
        font-weight: bold;
        color: var(--document-faint); color: {{ $t['faint'] }};
        border-bottom: {{ $fx(1) }} solid {{ $t['primary_ink'] }};
        letter-spacing: 0.08em;
    }
    table.items td {
        padding: {{ $fx(9) }} {{ $fx(8) }};
        font-size: {{ $s(Theme::GEOMETRY['text_base']) }};
        border-bottom: {{ $fx(0.4) }} solid {{ $t['rule'] }};
        vertical-align: top;
        {{--
            ورمزُ صنفٍ لا فراغَ فيه يُكسَر بدل أن يخرج.

            `SKU-ABAAD-2026-XL-0099` أو آيبانٌ بأربعة وعشرين حرفًا كلمةٌ
            واحدةٌ عند المحرّك: لا موضعَ كسرٍ فيها، فتمتدّ خارج عمودها
            وتُقصّ عند حافّة الورقة.
        --}}
        word-wrap: break-word;
        overflow-wrap: break-word;
    }
    /* واسمُ الصنف أقوى من رقمه: هو ما يُبحث عنه في السطر */
    table.items td.item { font-weight: bold; }
    /* والإجماليُّ آخرُ العمود وأثقلُه — إليه تنتهي قراءةُ السطر */
    table.items td.line { font-weight: bold; }

    /*
        الصفُّ لا ينكسر بين صفحتين، والرأسُ يتكرّر على كلّ صفحة.

        سطرٌ نصفُه هنا ونصفُه هناك يُقرأ مرّتين، وجدولٌ يمتدّ ثلاثَ صفحاتٍ
        بلا رؤوسِ أعمدةٍ في الثانية والثالثة يُقرأ بالتخمين: أيُّ عمودٍ
        الكميّةُ وأيُّها السعر.
    */
    table.items tr { page-break-inside: avoid; }
    table.items thead { display: table-header-group; }
    table.items tfoot { display: table-footer-group; }
    table.items td.empty {
        text-align: center;
        color: var(--document-faint); color: {{ $t['faint'] }};
        padding: {{ $fx(18) }} {{ $fx(7) }};
    }

    /* ————————————————— الإجماليّات ————————————————— */

    /*
        كتلةُ الإجماليّات لا تنكسر بين صفحتين.

        «الإجمالي» في صفحةٍ ومفرداتُه في أخرى يجعل من يراجع الورقة يعود
        صفحةً ليعرف ممّ تكوّن المبلغ. وهي كتلةٌ قصيرةٌ دائمًا، فحجزُها
        كاملةً لا يُضيّع صفحة.
    */
    {{--
        و`table-layout: fixed` شرطُ ما بداخله.

        عمودُ الملاحظات يحمل جدولًا بعرض ١٠٠٪ — انظر `.asidestack` — وجدولٌ
        كذلك داخل خليّةٍ ضيّقة يُربك حسابَ العرض الأدنى في mpdf فينسحق
        العمودان إلى حرفين (وهو العطبُ الذي أخرج لوحةَ المستند من الترويسة
        بعرض كلمتين). والعرضُ المثبَّت يُلغي ذلك الحسابَ من أصله: الأعمدةُ
        بما صُرّح لها لا بما يُقاس فيها.
    --}}
    .totals-wrap { width: 100%; table-layout: fixed; page-break-inside: avoid; }
    .totals-wrap > tbody > tr > td { border: none; padding: 0; vertical-align: top; }

    /*
        والمجاميعُ في لوحةٍ فاتحةٍ بلا إطار.

        ═══ ولمَ أرضيّةٌ بدل الحدّ ═══

        الإطارُ الرماديُّ حول المجاميع يرسم صندوقًا رابعًا على ورقةٍ نزعنا
        عنها الصناديق. وما يجمع المفرداتِ والإجماليَّ في شيءٍ يُقرأ دفعةً
        يفعله سطحٌ فاتحٌ جدًّا بلا خطٍّ حوله — وهو ما في المرجع المعتمد:
        «Totals panel فاتح».

        و`primary_wash` ستّةٌ بالمئة من لون التاجر: يكفي ليُرى سطحًا على
        الأبيض، ويبقى تحت النصّ بفارقِ تباينٍ واسع، ويحمل أثرَ لون التاجر
        بلا أن يصبغ شيئًا.

        والأرضيّةُ على الخليّة لا على كتلةٍ داخلها: mpdf يُقلّص كتلَ
        الـ`div` إلى نصّها داخل الخلايا — انظر `partials/parties`.
    */
    .totals-wrap td.totalcard {
        background: var(--document-primary-wash); background: {{ $t['primary_wash'] }};
        border-radius: {{ $fx($t['radius']) }};
        padding: {{ $fx(10) }} {{ $fx(12) }};
    }
    table.totals { width: 100%; border-collapse: collapse; }
    table.totals td {
        padding: {{ $fx(4) }} 0;
        font-size: {{ $s(Theme::GEOMETRY['text_base']) }};
        border: none;
        {{-- والمحاذاةُ مكتوبةٌ لا موروثة: انظر `table.idstack` --}}
        text-align: {{ $start }};
    }
    table.totals td.amt { text-align: {{ $end }}; }
    table.totals td.k { color: var(--document-muted); color: {{ $t['muted'] }}; }
    {{--
        وتلميحُ النسبة «(٥٪)» يبقى في سطر عنوانه.

        عمودُ العناوين ضيّق، و«ضريبة القيمة المضافة (٥٪)» يتجاوزه فينكسر
        التلميحُ سطرًا ثانيًا تحت العنوان — فيطول الصفُّ ويختلّ إيقاعُ
        الكتلة. والتلميحُ ثلاثةُ محارف: يُمنع كسرُه ويُترك للعنوان أن ينكسر.
    --}}
    table.totals td .xs { white-space: nowrap; }
    {{--
        والجانبيُّ يحاذي أعلى الكتلة صراحةً.

        `.totals-wrap > tbody > tr > td` تضبطه، لكنّ mpdf لا يُعوَّل عليه
        في محدّد الابن المباشر عبر `tbody` ضمنيّ — فتسقط القاعدةُ صامتةً
        ويحاذي الوسطَ، فيهبط السطرُ إلى منتصف المجاميع بلا سبب ظاهر.
    --}}
    .totals-wrap td.aside { padding-top: {{ $fx(2) }}; vertical-align: top; }
    /* وخليّةٌ فاصلةٌ بين العمودين: الفراغُ بلا `border-spacing` يُزيح الطرفين */
    .totals-wrap td.gap { padding: 0; font-size: 0; line-height: 0; }

    /*
        و«الإجمالي» أقوى عنصرٍ ماليٍّ في الورقة — بالمقاس والخطّ لا بالصبغ.

        ═══ ولمَ ذهبت الكتلةُ الداكنة ═══

        كان صندوقًا مصمتًا بلون التاجر ونصًّا معكوسًا عليه. وهو أثقلُ شيءٍ
        على الورقة: رقعةٌ سوداءُ في أسفلها تجعل الورقةَ كلَّها تُقرأ حولها،
        وتشرب حبرَ الطابعة، وتخرج من طابعةٍ اقتصاديّةٍ لطخةً لا رقمًا.
        والمرجعُ المعتمد صريح: «إزالة أي dark total card بالشكل الحالي».

        ═══ وكيف بقي أقوى ═══

        بثلاثةٍ لا تحتاج صبغًا: خطٌّ بحبر التاجر يفصله عمّا فوقه — وهو
        أثقلُ خطٍّ في اللوحة — ومقاسٌ أكبر من كلّ رقمٍ حوله، وحبرُ التاجر
        على الرقم نفسِه. فالعينُ تقع عليه أوّلًا وهو على أرضيّةٍ فاتحة.

        وتُركت أرضيّةُ الصفّ خاليةً عمدًا: القاعدةُ القديمة كانت تكتبها
        على الصفّ **وعلى خليّتيه معًا** لأنّ mpdf يرسم بينهما خيطًا
        أبيضَ رفيعًا فينشقّ الصندوقُ نصفين. ولا صندوقَ الآن فلا شقّ —
        والدرسُ محفوظٌ في `design-system/DESIGN_SYSTEM.md` §10 لمن يعيد
        أرضيّةً يومًا.
    */
    table.totals tr.grand td {
        font-size: {{ $s(Theme::GEOMETRY['text_lg']) }};
        font-weight: bold;
        color: var(--document-primary-ink); color: {{ $t['primary_ink'] }};
        padding-top: {{ $fx(7) }};
        padding-bottom: {{ $fx(2) }};
    }
    {{--
        وخطوطُ الفصل صفوفٌ بخليّةٍ واحدة — لا حدودٌ على خليّتَي الصفّ.

        خليّتان متجاورتان بحدٍّ واحدٍ يرسمهما mpdf مفصولتين بشعرةٍ بيضاء
        عند ملتقاهما، فينقطع الخطُّ في وسطه. انظر `partials/totals`.
    --}}
    table.totals tr.sep td {
        padding: {{ $fx(5) }} 0 0 0;
        border-top: {{ $fx(0.4) }} solid {{ $t['primary_edge'] }};
        font-size: 0;
        line-height: 0;
    }
    table.totals tr.sep td.strong { border-top-width: {{ $fx(1) }}; border-top-color: {{ $t['primary_ink'] }}; }
    /* والباقي بعد السداد: ثاني ما يُبحث عنه في ورقةٍ لم تُسدَّد كاملةً */
    table.totals tr.due td { font-weight: bold; }

    /* ————————————————— كتلٌ متفرّقة ————————————————— */

    {{--
        ═══ والملاحظاتُ: أين صارت تقع، وبأيّ شكل ═══

        كانت شريطًا بعرض الورقة أسفلَ المجاميع، له أرضيّةٌ رماديّةٌ وحافّةٌ
        سميكة: كتلةٌ رابعةٌ في ورقةٍ أسفلُها مزدحم. والمرجعُ المعتمد يضعها
        **في الجهة المقابلة للإجماليّات** — حيث كان نصفُ السطر خاليًا —
        والرمزَ أسفلها، ويُذهب أرضيّتَها مع سائر البطاقات.

        فلم يبق لها قاعدةٌ تخصّها: عنوانٌ صغيرٌ (`.eyebrow`) ونصٌّ تحته في
        صفّين من `table.asidestack` أعلاه. وما يفصلها عمّا حولها الفراغُ
        وحده، كما يفصل كلَّ شيءٍ آخر على هذه الورقة.
    --}}
    {{--
        عمودُ الملاحظات — كتلٌ متراكبةٌ يفصل بينها حشوُ خلاياها.

        ═══ ولمَ جدولٌ لا كتلُ `div` ═══

        قِيس في الـPDF: **mpdf يُسقط هوامشَ كتل الـ`div` داخل خليّة جدول**
        — `margin-top` و`margin-bottom` معًا — فتتراكم الملاحظاتُ وعنوانُها
        وعددُ الأصناف ثلاثةَ أسطرٍ متلاصقة. والحشوُ لا يُنقذها: يُقلَّص معها.
        وجُرّب `line-height` فأعطى جزءًا ممّا صُرّح به لا كلَّه.

        وحشوُ خليّةِ الجدول يُحترم كاملًا — وهو ما تقوم عليه `metastrip`
        و`totalcard` أصلًا. فما لا يُضبط بكتلةٍ يُضبط بصفّ.
    --}}
    table.asidestack { width: 100%; border-collapse: collapse; }
    {{-- والمحاذاةُ مكتوبةٌ لا موروثة: انظر `table.idstack` --}}
    table.asidestack td { border: none; padding: 0 0 {{ $fx(11) }} 0; vertical-align: top; text-align: {{ $start }}; }
    table.asidestack tr:last-child td { padding-bottom: 0; }
    /* وعنوانُ الكتلة ألصقُ بجسده منه بما قبله */
    table.asidestack td.cap { padding-bottom: {{ $fx(2.5) }}; }

    /*
        وخانةُ التوقيع خانةٌ فعلًا — سطرٌ منقوطٌ يُكتب عليه.

        خطٌّ متّصلٌ رفيعٌ تحت الكلمة يُقرأ فاصلًا بين قسمين لا موضعَ توقيع.
        والمنقوطُ لا يُقرأ إلّا موضعَ كتابة — وهو ما تفعله كلُّ استمارةٍ
        تُوقَّع.
    */
    /*
        وخانةُ التوقيع صندوقٌ فعلًا — لا كلمةً معلّقةً في بياض.

        والإطارُ على الخليّة لا على كتلةٍ داخلها: mpdf يُقلّص كتلَ الـ`div`
        إلى نصّها داخل الخلايا، فيخرج الصندوقُ بعرض الكلمة لا بعرض ما
        يُوقَّع عليه. انظر `partials/parties`.
    */
    table.sign { width: 100%; border-collapse: separate; border-spacing: 0; margin-top: {{ $fx(24) }}; page-break-inside: avoid; }
    table.sign td.gap { width: {{ $fx(14) }}; }
    {{--
        وصارت خطًّا يُوقَّع فوقه لا صندوقًا منقوطًا حوله.

        الصندوقُ المنقوط كان يحلّ مشكلةً حقيقيّة — «كلمةٌ معلّقةٌ في بياض»
        لا تُقرأ موضعَ توقيع. لكنّه صندوقٌ رابعٌ على ورقةٍ نُزعت عنها
        الصناديق. والحلُّ الذي تستعمله كلُّ استمارةٍ تُوقَّع: فراغٌ محجوز،
        ثمّ خطٌّ، ثمّ اسمُ ما يُوقَّع تحته. فيُقرأ موضعًا بلا إطار.
    --}}
    table.sign td.box {
        border-top: {{ $fx(0.5) }} solid {{ $t['border'] }};
        padding: {{ $fx(5) }} 0 0 0;
        vertical-align: top;
        color: var(--document-faint); color: {{ $t['faint'] }};
    }
    /* والفراغُ فوق الخطّ هو ما يُوقَّع فيه: خليّةٌ خاويةٌ بارتفاعٍ محجوز */
    table.sign td.space { border: none; padding: 0; height: {{ $fx(26) }}; font-size: 0; line-height: 0; }

    /* والرمزُ لا يركب على الجدول: كتلةٌ محجوزةٌ كاملةً */
    .qr-block { margin-top: {{ $fx(10) }}; page-break-inside: avoid; }
    .qr-cap {
        font-size: {{ $s(Theme::GEOMETRY['text_xs']) }};
        color: var(--document-faint); color: {{ $t['faint'] }};
        margin-top: {{ $fx(2) }};
        line-height: 1.35;
    }

    .foot {
        margin-top: {{ $fx(18) }};
        padding-top: {{ $fx(8) }};
        border-top: {{ $fx(0.4) }} solid {{ $t['border'] }};
        color: var(--document-muted); color: {{ $t['muted'] }};
        font-size: {{ $s(Theme::GEOMETRY['text_sm']) }};
        page-break-inside: avoid;
    }

    /*
        وتذييلُ الهويّة — آخرُ ما على الورقة، وما يجعل الصفحةَ الضائعة تُعرف.

        صفحةٌ ثانيةٌ من فاتورةٍ تسقط من ملفّ: بلا اسم متجرٍ ورقمِ مستندٍ
        عليها لا يعرف أحدٌ إلى أين تعود. فيحمل الشريطُ الثلاثة: من، وأيّ
        مستند، وأيّ صفحة.
    */
    {{--
        وخطُّه شعرةٌ محايدة — «Footer رفيع جدًا» في المرجع المعتمد.

        وكان تسعةَ أعشارِ نقطةٍ بلون التاجر: خطٌّ يُنافس خطَّ الإجمالي فوقه
        بقليل، وآخرُ ما على الورقة لا ينبغي أن يُنافس شيئًا.
    --}}
    table.docfoot {
        width: 100%;
        border-collapse: collapse;
        margin-top: {{ $fx(20) }};
        border-top: {{ $fx(0.4) }} solid {{ $t['border'] }};
        page-break-inside: avoid;
    }
    table.docfoot td {
        border: none;
        padding: {{ $fx(6) }} 0 0 0;
        font-size: {{ $s(Theme::GEOMETRY['text_xs']) }};
        color: var(--document-faint); color: {{ $t['faint'] }};
        vertical-align: top;
    }
</style>
