import { NotFoundState } from '@/components/States';

/**
 * ٤٠٤ — بالحالة الصحيحة، ومرسومةً على الخادم.
 *
 * صفحةٌ تقول «غير موجودة» وتردّ ٢٠٠ يفهرسها غوغل صفحةً حقيقية، فتظهر في
 * نتائج بحثٍ عن اسم المتجر. و`notFound()` في Next هي ما يجعل الحالة ٤٠٤.
 *
 * وهذه الصفحة **ساكنة عمدًا**: لا تقرأ الترويسات ولا المستند. جُرِّبت وهي
 * تقرؤهما — لترسم ٤٠٤ بهويّة المتجر — فكان Next يسقط إلى قشرة خطأ تُملأ في
 * المتصفّح، فيصل الزاحفَ ومن لا JavaScript عنده صفحةٌ فارغة. وصفحةٌ محايدةٌ
 * تُرسم خيرٌ من صفحةٍ جميلةٍ لا تُرسم.
 */
export default function NotFound() {
    return <NotFoundState />;
}
